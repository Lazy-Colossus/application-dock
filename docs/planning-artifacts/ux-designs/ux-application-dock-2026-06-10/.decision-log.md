# Decision Log — Hotaru UX

Canonical memory and audit trail for Hotaru's UX (DESIGN.md + EXPERIENCE.md). Spines win on conflict.

## 2026-06-10

- **Intent:** Create. UX specs for "Hotaru" — Japanese vocabulary app, a new app inside the Application Dock platform.
- **Sources (inherit by reference; do not duplicate):**
  - PRD (primary): `docs/planning-artifacts/prds/prd-application-dock-2026-06-10/prd.md` (final)
  - Brief: `docs/planning-artifacts/briefs/brief-application-dock-2026-06-10/brief.md`
  - Brainstorm: `docs/brainstorming/brainstorming-session-2026-06-10-0041.md`
- **Inherited UI system:** Application Dock front-end is **Vue 3 + Quasar v2** (Material-based). Both spines inherit from Quasar; DESIGN.md extends Quasar tokens, EXPERIENCE.md specifies behavioral delta only. Existing sibling app (Archery Score Counter) is a Quasar precedent to match for platform consistency.
- **Form-factor (confirmed):** **mobile web only — phone viewport.** Design single-column for phone; no wider-screen/desktop layouts. (Couch + bus use are both on the phone.)
- **Working mode:** Coaching path — walk look-and-feel decisions together, weave in creative tools (colour themes, design directions, wireframes, key-screen mocks).
- **Chosen creative vision (verbatim intent):** Serene but playful; lean into Hotaru (firefly / 蛍雪); late-evening/midnight dark-blue base with gentle playful accent colours + glow effects, between firefly light and evening neon; actual fireflies drift/pop up on screen.
- **Design directions rendered** (`.working/`, hero = Practice Session drill screen, pure-CSS fireflies):
  - `direction-firefly-midnight.html` — balanced centre: midnight indigo, warm firefly-amber + soft-teal, glassy cards, gentle fireflies.
  - `direction-neon-yugure.html` — evening-neon/dusk end: darker, saturated cyan/magenta/violet bloom, more sparkle.
  - `direction-hotaru-glow.html` — serene-zen end: restrained, single amber glow, light type, sparse slow fireflies, lots of space.
  - Each also shows the pre-session picker (Genki · Lesson 2 + 5-tier familiarity ramp, shape+label so colour isn't sole signal) and an extra state.
- **User feedback round 1:** likes `firefly-midnight` and `neon-yugure` — KEEP both as finalists for later comparison. Wanted to explore a different angle too.
- **Second angle explored — "playful zen forest" (verbatim intent):** beige + bamboo green, studying in a calm forest with Japanese moss-covered stone garden lanterns (灯籠) lighting up at dusk, fireflies flying around. Familiarity ramp = soft pastel red→orange→green.
- **Forest directions rendered** (`.working/`, same hero/secondary/state structure, pastel ramp rose→peach→yellow-green→light-green→bamboo with shape glyphs ○◔◑◕●):
  - `direction-forest-lantern-dusk.html` — balanced centre: warm sand, bamboo green, amber lantern glow at dusk, drifting fireflies.
  - `direction-forest-paper-day.html` — airy zen-light: washi-paper beige, daytime forest, dim lanterns, sparse fireflies.
  - `direction-forest-twilight-deep.html` — playful deeper dusk: dark mossy field, beige cards, stronger lantern bloom, more fireflies.
  - PENDING user pick/blend across all 5 finalists.
- **User feedback round 2:** forest set not quite hitting the vision; **dark green background works**; likes the lantern idea but wanted an accurate *traditional Japanese stone lantern (tōrō / 石灯籠)*, per https://en.wikipedia.org/wiki/Stone_lantern (anatomy: kiso base → sao post → chūdai platform → hibukuro fire box w/ sun-moon windows glowing → kasa roof w/ upturned warabide corners → hōju finial).
- **Stone-lantern revision rendered** (`.working/`, dark mossy-green base, inline-SVG accurate tōrō glowing from fire box, CSS fireflies, pastel ramp):
  - `direction-toro-kasuga.html` — tall slender kasuga-dōrō pedestal lantern, elegant vertical silhouette.
  - `direction-toro-yukimi.html` — low broad yukimi-dōrō "snow-viewing" lantern on curved legs, wide umbrella roof.
- **Composition fix:** lantern was hidden behind the drill card. Revised both files — frosted-glass card (backdrop blur) so lantern reads through, lower third left open, and lantern made PROMINENT on calmer screens (picker, rest state) where it naturally shines. Insight: on the dense drill screen the lantern is necessarily ambient; it's the hero motif on calm screens.
- **DIRECTION DECISION:** user chose the **dark mossy-green forest + Yukimi-dōrō (low broad snow-viewing stone lantern)** world. The prior yukimi *drawing* wasn't to their liking → isolating the lantern artwork decision before proceeding.
- **Lantern gallery rendered:** `.working/lanterns-gallery.html` — 5 inline-SVG stone lanterns on dark green, glowing fire box + fireflies: (1) Yukimi classic hexagonal, (2) Yukimi round-dome, (3) Yukimi tall-roof six-legged, (4) Yukimi minimal zen icon, (5) Kotoji-dōrō wildcard.
  - User reaction: all 5 looked "like robots" (too geometric/CAD). Need a traditional, soft, hand-illustrated feel.
- **User art-direction prompt (verbatim — keep for DESIGN.md inspiration):** "A minimalist Japanese stone lantern (yukimi-dōrō style, low garden type) softly glowing from within, flat illustrative style for a mobile app. Weathered grey granite with a mossy green cap. Warm amber candlelight from the openings. Fireflies drifting, faint yellowish-green glow. Japanese forest floor at summer twilight — deep indigo and teal sky through silhouetted bamboo and pine. Serene, meditative. Minimal detail, soft edges, muted palette: slate grey, moss green, deep indigo, warm amber. 2:3 portrait, no text."
- **Medium note:** environment can't generate AI raster art; delivering flat-illustration **inline-SVG** scenes interpreting the prompt (scalable, app-ready). User may alternatively run the prompt through an external image model → drop into `imports/` (design-handoff path).
- **Illustrated lantern gallery rendered:** `.working/lanterns-illustrated.html` — 5 flat-illustration SVG twilight scenes (organic Bézier granite, soft glows, layered sky→bamboo→floor): (1) faithful brief, (2) ultra-minimal, (3) lusher forest, (4) closer crop, (5) softer/warmer palette.
- **LANTERN DROPPED:** user decided to drop the lantern entirely. New direction: base on `direction-forest-twilight-deep`, remove lantern, keep ONLY background fireflies, make the drill card **full-bleed (spans whole screen)**.
- **Lanternless firefly directions rendered** (`.working/`, full-bleed drill, CSS fireflies, pastel ramp, no lantern):
  - `direction-forest-fireflies-a.html` — faithful to forest-twilight-deep (dark mossy green-brown, beige+bamboo, original firefly density).
  - `direction-forest-fireflies-b.html` — cool cousin: teal/blue twilight, brighter/denser fireflies.
  - `direction-forest-fireflies-c.html` — warm earthy cousin: moss green-brown, bamboo-green primary accent, calmer/sparser fireflies.
  - **User picked `direction-forest-fireflies-c`** (warm earthy moss green-brown, bamboo-green primary accent, calm sparse fireflies, full-bleed) as a strong finalist. Wants to see one more route (woodblock) before final pick.
- **Final route explored — woodblock / ukiyo-e:** user idea (not fully formed) — Japanese woodblock-print inspired, possibly real prints as backgrounds. Noted: classic Hokusai/Hiroshige are public domain; real prints can go in `imports/` later with a legibility scrim; for now recreate STYLE in SVG. Ties to name via "hotaru-gari" (firefly-catching) ukiyo-e theme. Legibility approach: woodblock backdrop + calm washi content panel.
- **Ukiyo-e directions rendered** (`.working/`, inline-SVG flat planes/bold outlines/bokashi/seigaiha+asanoha, washi content panel, CSS fireflies, pastel ramp harmonized):
  - `direction-ukiyoe-twilight.html` — Hiroshige evening landscape (bokashi dusk sky, moon, hills/Fuji, seigaiha band).
  - `direction-ukiyoe-aizuri.html` — aizuri-e blue monochrome Great-Wave motif, off-white washi panel.
  - `direction-ukiyoe-hotarugari.html` — firefly-catching summer night (most thematic to 蛍): indigo-teal night, reeds/bamboo, figure w/ uchiwa fan, asanoha, glowing fireflies.
  - PENDING final pick across all routes (forest-fireflies-c vs a ukiyo-e). After lock → distil DESIGN.md, proceed to app screens.
- **🔒 VISUAL DIRECTION LOCKED:** `direction-forest-fireflies-c` is the chosen foundation.
  - Mood: calm, warm, quietly alive (serene-playful). No lantern. Ukiyo-e and midnight routes set aside (kept in `.working/` for reference).
  - Palette: warm earthy **moss green-brown** twilight field; **bamboo-green** primary accent; **beige/washi** surfaces; warm firefly-amber glow accents.
  - Motion: sparse, slow drifting CSS **fireflies** in the background (with prefers-reduced-motion fallback).
  - Layout: **full-bleed** screens (no floating-card-in-frame); translucent washi reveal panel for drill answers.
  - Familiarity ramp: pastel rose→peach→yellow-green→light-green→bamboo, paired with shape glyphs ○◔◑◕● + labels (colour never the sole signal — accessibility).
  - To promote to `mockups/` at Finalize; distil into DESIGN.md tokens (extending Quasar).
- **IA decisions:** (1) **Topics fold into the Library** as a filter/tab (no standalone screen). (2) **Identity:** a "who's studying?" select screen at session start; thereafter a **top-bar current-user avatar** that opens a menu to switch user + access settings.
- **IA surface map (closes all PRD needs):** Home/identity → app home w/ avatar switcher; Practice picker + pre-session overview (mocked); Drill flow (front→reveal→grade, typed variant); Session summary/rest (mocked); Library/word list (browse by Lesson + Topic filter, familiarity colours, shared/private, add); Add-word (manual entry); Word detail + shared notes.
- **Key screens rendered** (`.working/`, locked forest-fireflies-c style):
  - `key-home.html` — "Who's studying?" (Dani/Jake) + Jake's home with avatar menu open (Switch user · Settings) over Practice/Library.
  - `key-library.html` — shared Master Vocab list, Lesson + Topic filter tabs, familiarity glyph+dot rows, shared/private marks (漫画 = Dani private), ＋add FAB.
  - `key-add-word.html` — low-friction entry: 3 required fields (Japanese/reading/meaning) + optional POS/scope + Shared/Private toggle.
  - `key-word-notes.html` — 約束 detail + Jake's attributed shared mnemonic + viewer private note; add-note with Shared/Private toggle (UJ-3).
  - PENDING user reaction; then distil spines + Finalize.
- **Refinement notes round 1 (apply to locked direction + all key screens):**
  1. **Fireflies fly around** the screen on gentle wandering paths (not just pulse in place), each with a **very subtle soft glow** halo. (Keep prefers-reduced-motion fallback.)
  2. **Glow around the practiced Japanese characters** on the drill card (soft warm amber), and **make the word card stand out more** — it currently doesn't pop enough vs the background.
  3. **Smaller category/filter pills** (Lesson/Topic tabs in Library; the scope pill in the picker).
  4. **Word-detail (Section A) must already include an "Add note" affordance** (not only in the alternate add-note state).
  5. **Notes display:** do NOT label notes as "shared" (shared is the implicit default). Show ONLY a **🔒 lock icon on private notes**. (Share/Private toggle still exists when creating a note.)
  - APPLIED to all 5 mocks: fireflies wander on looping paths w/ soft glow halos (reduced-motion fallback kept); practiced JP word has warm amber text-glow + drill card elevated to focal; Library filter pills + picker scope pill made smaller; word-notes Section A has inline "＋ Add note"; notes show no "shared" badge, only 🔒 on private. These refinements feed DESIGN.md (glow tokens, elevation, pill sizing, firefly motion) + EXPERIENCE.md (notes display rule, add-note affordance).
- **Refinement notes round 2:**
  1. **Fireflies ~50% dimmer over the main cards** (distracting there) — render fireflies BEHIND content panels so the translucent card dims them; full brightness over plain background. Pure CSS (z-index + panel translucency).
  2. **Category pills even smaller, lined along the BOTTOM EDGE of the word card.** On overflow, collapse to an icon + "＋N more" with an expand toggle (CSS checkbox-hack, no JS).
  3. **Familiarity ≠ category pill** (same pill style was misleading). Familiarity becomes a **distinct ICON per level** (separate from category pills). Actual icon set TBD later; for now nail size/placement using a suitable icon for 'Strong'. Apply consistently on drill card, Library rows, word-detail header.
  4. **Word card itself gets a subtle glow** (not just the characters).
  5. **The practiced word in a more vibrant colour** so it pops (on-palette vibrant accent — e.g. luminous amber/gold or bright bamboo-green).
  - APPLIED: fireflies now sit behind translucent panels (≈half-bright over cards); drill card got ambient amber glow; practiced word 約束 recoloured vibrant amber-gold (#ffd98a, brightest element); familiarity split from category pills into a distinct glowing ICON (placeholder ◉/ramp, final icon set TBD); category pills tiny along card bottom edge with CSS-only ＋N expand toggle. Applied across direction-c (drill), key-library (familiarity icon ramp), key-word-notes (header icon + bottom pills); firefly-dimming also on key-home/key-add-word.
- **Refinement note round 3:** practiced word colour → **warm glowing lamp-yellow** "like a lamp glowing in the dusk" (#ffd24a + warm yellow glow). APPLIED to drill card.
- **Refinement note round 4 (microcopy + voice):**
  - Scoring buttons: drop the per-grade subtext — just **Incorrect / Close / Correct**. APPLIED.
  - "Begin a calm round →" → **"Let's practice →"**. APPLIED.
  - "Worth a gentle revisit" → **"Worth a revisit"**. APPLIED.
  - Home Practice action "A gentle round…" → **"Drill the words you're learning"**. APPLIED.
  - **VOICE PRINCIPLE (carry into EXPERIENCE.md Voice & Tone):** do NOT over-index on calm/gentle wording — calm is the manner, not the mission. Voice = warm, clear, encouraging toward effective practice; avoid "calm/gentle/serene" filler. (Remaining poetic footers like "the night is patient 🌙" to be re-judged when EXPERIENCE.md Voice & Tone is distilled.)

## FINALIZE (2026-06-12)

- **Mocks promoted** to `mockups/`: drill-and-picker, home, library, add-word, word-notes (the locked forest-fireflies-c keepers). Other explored directions remain in `.working/` for reference.
- **Spines distilled:** `DESIGN.md` (visual contract — palette incl. lamp-yellow #ffd24a + moss/bamboo/beige/firefly, type w/ CJK note, elevation/glow, firefly motion, familiarity-icon ramp, components, Do's/Don'ts; extends Quasar) and `EXPERIENCE.md` (Foundation, IA, Voice&Tone w/ calm-is-manner principle, Component+State patterns, Accessibility floor, Key Flows UJ-1/2/3). Both inline-link the mockups; spines win on conflict.
- **Input reconciliation** (`reconcile-prd.md`): coverage strong (all 24 FRs homed, UJ-1/2/3 present, soul preserved). 5 minor gaps fixed: FR-6 Topic create/assign affordance; FR-23 flip note visibility later; reserved Phase II "✨ improve" slot in word-detail; named the 蛍雪 study-lamp metaphor in DESIGN Brand & Style; aligned 10 EXPERIENCE token refs to DESIGN's kebab token paths.
- **Open items triaged (deferred, non-blocking):** (1) Japanese display font — system CJK fallback for now; pick a Japanese-capable font before build. (2) Familiarity ICON set — size/placement locked, actual glyphs TBD. (3) Settings menu contents — entry exists, contents unspecified (no v1 FRs). None block architecture/stories.
- **Reviewer Gate:** offered; adversarial lens pass SKIPPED by default given hobby stakes + already-reviewed PRD + clean reconciliation. Available on request.
- **Status:** DESIGN.md + EXPERIENCE.md → `final`.

## UPDATE — Pivot to Neon Yūgure (2026-07-06)

- **DECISION (Your Highness):** the canonical visual identity pivots from *forest-twilight fireflies* to **Neon Yūgure (夕暮れ / dusk)** — the evening-neon direction previously explored in `.working/direction-neon-yugure.html`. Same 蛍/firefly soul and no-pressure stance; new skin.
  1. **Status:** Neon Yūgure becomes THE identity in `DESIGN.md`. Forest-twilight is preserved as a documented **backup appendix** (and in git commit `c8458eb`).
  2. **Depth:** **full component parity** — every planned surface re-specified in neon (drill card, FamiliarityIcon ramp, category pills + ＋N overflow, grade buttons, note callouts, picker, session summary, word-detail, app-bar/avatar, word row, glass panels, firefly layer).
  3. **Mocks:** fresh neon key-screen HTML mocks rendered (home, library, drill+picker, word-notes).
- **Palette (from the neon direction):** deep-indigo dusk **field** (`#04060f`→`#0a0a24`→`#1a1148` radial), violet-tinted **glass** panels (`rgba(20,18,52,.55)`, `blur(16px)`), neon accents **cyan `#38f0e6` (PRIMARY)** · **magenta `#ff5cc8`** · **violet `#9b6bff`** · firefly **amber `#ffce5c`**; ink `#f1f0ff`/`#b3aede`/`#6f6aa0`. The **practiced word glows cyan** (replacing forest lamp-yellow). Familiarity ramp: new `#5a5a86` ○ · learning `#9b6bff` ◔ · familiar `#38f0e6` ◑ · strong `#ffce5c` ◕ · mastered `#ff5cc8` ●.
- **Token re-point:** the single semantic accent `{colors.bamboo}` used across EXPERIENCE.md → `{colors.cyan}`; behaviour/IA/flows unchanged.
- **Carried unchanged from the forest identity:** the calm-is-the-manner voice principle; colour-never-alone familiarity (icon + colour + label); queue-not-debt (no streaks/due-counts); full-bleed single-column phone; Quasar inheritance; fireflies sparse-behind-panels with a `prefers-reduced-motion` fallback (now multi-colour + brighter bloom).
- **Already applied in code** (ahead of the docs): `hotaru.sass` neon tokens, glass `.hotaru-panel`, cyan `.hotaru-glow`, multi-colour `FireflyLayer`, and the neon sweep across all live pages (commit pending).

### FINALIZE — Neon Yūgure (2026-07-06)

- **Spines updated:** `DESIGN.md` rewritten as the canonical **Neon Yūgure** identity (full frontmatter tokens + body; full component parity: app-bar/avatar, glass panel, drill card, familiarity-icon ramp, category pills + ＋N, grade buttons, CTA/FAB, word row, note callouts shared/private, firefly layer) with an Appendix documenting the forest backup + revert path. `EXPERIENCE.md` token re-pointed (`{colors.bamboo}`→`{colors.cyan}`) and its Visual-reference note switched to neon; behaviour/IA/flows/voice unchanged.
- **Neon mocks rendered & promoted** to `mockups/` (canonical): home, library, drill-and-picker, word-notes, add-word. The superseded forest mocks moved to `mockups/forest/` as backup; the neon-direction explorations remain under `.working/neon-*` and `.working/direction-*`.
- **Mock coverage:** all five canonical surfaces mocked in neon. Identity is covered within Home. **Session Summary (Story 2.7)** and the standalone **Word-detail** header are spine-only for now (word-detail is represented within `word-notes`); render on request when those stories come up.
- **Reviewer Gate:** offered; not run (hobby stakes, behaviour unchanged from an already-reviewed spine, palette-only pivot). Available on request (adversarial / accessibility lenses).
- **Implementation status:** the running app already wears this skin (`apps/hotaru/css/hotaru.sass` neon tokens, glass `.hotaru-panel`, cyan `.hotaru-glow`, multi-colour `FireflyLayer`, neon sweep across pages) — code commit still pending.
- **Status:** DESIGN.md + EXPERIENCE.md remain `final`, `updated: 2026-07-06`.

### UPDATE — Word-row & filter-pill colour rules (2026-07-06)

- **Kanji/kana colour split (word list):** kanji headwords glow **cyan `#38f0e6`**; kana glyphs glow **lamp-yellow `#ffd24a`** — both a kana-only headword and the hiragana reading line. Added `colors.lamp-yellow #ffd24a` as a distinct token (the 蛍 brand logo also uses it). Mirrors the drill card (Japanese = cyan) while giving kana its own warm read.
- **Library filter hierarchy:** level-1 **sections = cyan** (primary); level-2 **subsections = violet** (tertiary accent → subordinate) and smaller. UX rationale (Sally): yellow subsections inverted the hierarchy (highest-luminance child), echoed the new kana-yellow, and overloaded the amber accent; violet is the calm, subordinate, low-collision cool accent already present in the glass/ambient surface. Added a `library-tabs` component to DESIGN.md.
- Captured in `DESIGN.md`: new `lamp-yellow` colour token, `library-tabs` + reworked `word-row` component tokens, Colors + Components prose, and two Do/Don't rows.
