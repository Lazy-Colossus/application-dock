# KDH UX — Decision Log

Canonical record of what was decided and why. `DESIGN.md` and `EXPERIENCE.md`
are distilled from this at Finalize; both win over any mock on conflict.

**Stakes:** hobby — six friends, self-hosted, no compliance surface.
**Mode:** Create. Workspace bound 2026-09-03.

---

## Entry 1 — Workspace naming deviates from the skill pattern

The configured `run_folder_pattern` is `ux-{project_name}-{date}`, which would
give `ux-application-dock-2026-09-03` and read as a second platform-wide run.
This session is app-scoped, so the folder is `ux-kdh-2026-09-03`, matching how
`epics-kdh.md` is named. Pattern deviation is deliberate.

## Entry 2 — Sources confirmed

All four candidates apply:
- `docs/superpowers/specs/2026-09-02-kdh-design.md`
- `docs/planning-artifacts/epics-kdh.md`
- `docs/planning-artifacts/ux-designs/ux-application-dock-2026-06-10/`
- `docs/brainstorming/brainstorming-session-2026-09-02-0129.md`

## Entry 3 — FINDING: the "platform spine" is not a platform spine

`ux-designs/ux-application-dock-2026-06-10/` is named for the platform but both
its files are titled **Hotaru** and describe **Neon Yūgure** — a deep-indigo
dusk field with cyan/magenta/violet neon, violet glass panels, drifting
fireflies, **phone-viewport only, full-bleed**. That is one app's identity, not
the dock's.

**KDH therefore inherits nothing from it.** What KDH actually inherits is the
shell's own theme, which lives in code rather than in any UX document:

`frontend/src/css/quasar.variables.sass` — **"Carbon"**
| Token | Value | Role |
|---|---|---|
| `surface-base` | `#141414` | near-black page |
| `surface-raised` | `#1E1E1E` | raised surface |
| `surface-card` | `#242424` | card fill |
| `ink-primary` | `#F0F0F0` | body text |
| `ink-secondary` | `#8A8A8A` | secondary text |
| `ink-disabled` | `#4A4A4A` | disabled |
| `accent` (= `primary`) | `#C8960A` | muted gold — the single accent |
| `border-color` | `#2A2A2A` | hairline borders |
| `confirmed` / `positive` | `#2E7D32` | success |
| `error` / `negative` | `#CF6679` | error |

Consequence for this session: the coverage heat ramp has to live inside a
**near-black, single-gold-accent** world, not a light one. Any palette decision
is made against `#141414`, and gold is already spoken for as "the interactive
accent" across the dock.

## Entry 4 — GAP: form factor was never specified

The KDH design spec states no form factor. The two readings in it pull in
opposite directions: "unmistakable across a room" implies a wide screen, while
six people voting from a shared link will realistically do it on phones. Every
other decision in Epic 3 — how many names fit in a cell, whether `+N` overflow
is the common case or the exception, whether the heat ramp or the count is the
primary signal — depends on this. Must resolve before IA closes.

Note: Listies pinned this explicitly (NFR-1, desktop/wide-first). KDH did not.

## Entry 5 — DECIDED: phone-first, one layout

Everyone does everything on a phone, admins included. One layout, no desktop
variant. Matches Hotaru's form factor and the reality of six people opening a
link from a group chat.

## Entry 6 — CONSEQUENCE: FR-13 as written cannot hold

FR-13 says *"Each day cell lists, in small text, the names of the invitees on
it"*, with a `+N` remainder above an overflow threshold. That was written
assuming a wide cell. A phone month is seven columns across ~360px — roughly
**48px per cell**. Names do not fit; even one 4-letter name at a legible size
is marginal, and six is impossible.

So on a phone the `+N` overflow is not the exception, it is **always** the case,
which makes the whole names-in-cell mechanic dead weight.

**Resolution to confirm with the user:** the cell carries the signals that
survive at 48px — the date, the coverage **count**, and the **heat** — and the
names move to a **day detail** surface opened by tapping the cell, which is
also where a claimed person sets their own availability. This turns FR-13 from
"names in the cell" into "names one tap away", and adds a surface the IA did
not previously have.

Knock-on: NFR-6's *"unmistakable across a room"* is a desktop idea. On a phone
the bar is *readable at arm's length*, one-handed, outdoors.

## Entry 7 — DECIDED: its own identity, but very minimal

Not the dock's Carbon theme, and not a Hotaru-scale expressive world. KDH gets
its own visual language whose defining quality is **restraint** — few elements,
generous space, and colour used sparingly enough that the heat ramp is the
loudest thing on the screen.

Open: field colour (light or dark), and how coverage is expressed. Being
elicited via rendered directions rather than described in prose.

## Entry 8 — DECIDED: the "Wash" encoding

Coverage is the cell's background; the month reads as a field where the good
days are brighter. Chosen over "Fill" (a vessel filling from the bottom) and
"Ring" (a dial per day).

Accepted trade-offs, recorded so they are not rediscovered as bugs:
- Four-of-six and five-of-six are hard to separate by wash alone. **The count
  stays in the cell** to settle it — it is not decoration.
- Wash leans hardest of the three on colour, so the count and the chosen-day
  shape are what carry the meaning for a colour-blind reader.

## Entry 9 — DECIDED: purple world — dark ground, eggplant → pastel ramp

Wash re-skinned at the user's direction: violet-black field, the ramp running
from deep eggplant at low coverage to pastel lilac at full.

## Entry 10 — CONSEQUENCE: the invitee palette must leave the purple family

Two different things are encoded by colour and they must not be confused:
- **the ramp** answers *how many can come* — purple, ordered, low to high;
- **an invitee colour** answers *which person* — categorical, unordered.

If invitees were also purple, a person's colour would read as a coverage level.
So invitee colours become **varied pastels** — rose, sky, mint, sand, lilac,
aqua — desaturated enough to sit on the violet ground without competing with
the ramp for attention.

This obsoletes the 12-hue palette currently shipped in
`kdh_service.PALETTE` (ember, cobalt, violet, jade, magenta, amber…), which was
picked before any visual direction existed and is too saturated for this world.
Replacing it is a small change to one tuple, but it is a change to code already
merged in Story 1.3 — noted for Finalize.

## Entry 11 — DECIDED: header dropdown for claiming a name

Chosen over the bottom sheet (biggest targets, but covers the month) and the
chip rail (never hides the roster, but eats permanent vertical space and wraps
badly past ~8 people).

Consequences:
- The month stays visible while picking — the reason it won.
- It is the natural home for the colour swatches, so **FR-9 recolouring lives
  inside the same control** rather than needing a surface of its own.
- Targets are the smallest of the three, so the rows must be at least 44px and
  the control must not sit flush against the top edge.

## Entry 12 — ASSUMPTION: low end of the ramp needs more separation

Not confirmed by the user. Steps 0 (`#221B2E`) and 1 (`#2B2040`) are close
enough that "one person is free" may read as "nobody". Step 1 is lightened to
`#31234A` in the spine so the first vote is visibly a change; the count is the
backstop either way. Overturnable.

## Entry 13 — Upstream reconciled to the spines

The user ruled that this session's decisions take precedence over the documents
written before it. Changes made:

**`docs/superpowers/specs/2026-09-02-kdh-design.md`**
- **NFR-0 added** — phone-only, one layout. The spec previously stated no form
  factor at all; this is the constraint the rest follows from.
- **FR-13 rewritten** — names moved from the cell to the day sheet.
- **NFR-6 rewritten** — "unmistakable across a room" became "at arm's length,
  one-handed, on a phone", plus the explicit 4.5:1 contrast floor.
- *Interaction design* and *Visual direction* rewritten to match, both now
  deferring to the spines as the winning contract.
- Frontend structure: `InviteePanel` → `NameDropdown`, `AdminBar` → `AdminMenu`,
  and `DaySheet` added.

**`docs/planning-artifacts/epics-kdh.md`**
- Same FR-13 / NFR-6 / NFR-0 changes; UX spines added to `inputDocuments`.
- **Story 3.3 rewritten** from "See who is on each day" (names in the cell) to
  "The day sheet — who is on this date".
- Stories 2.3 and 2.4 now specify the header dropdown and put the colour
  swatches inside it; 3.1 records arrows-only; 3.2 says the cell carries date and
  count only, and that an unclaimed tap opens the dropdown; 3.4 and 4.1 name the
  actual tokens and marks.
- `useHeatScale` → `useWashScale` throughout, matching the identity.

**`backend/app/services/kdh_service.py`**
- `PALETTE` replaced: twelve saturated hues → the eight pastels, with a comment
  recording that invitee colours are categorical and must stay outside the wash's
  purple family. Backend 863 and frontend 1131 stay green — the palette's tests
  assert uniqueness, ordering and exhaustion rather than specific values, so they
  covered the swap without modification.


## Entry 14 — Roster visibility raised, at the user's direction (2026-09-03)

Change request after seeing the app running:

1. **The name control is far more prominent, and reads as unanswered.** Unclaimed
   it is a negative-bordered pill with a warning glyph — a required field left
   blank — because the calendar is read-only until it is answered, and the previous
   quiet text link did not say so.
2. **The calendar page shows a headcount** beside the calendar's name.
3. **The calendar list row shows the headcount beside the name** (person icon +
   number) and moves **the invitees' names** into the subtext where the count used
   to be, trailing off past the sixth.

Consequence: `CalendarSummary` gains `invitee_names`, so the list can show who is
invited without a request per row. This supersedes part of Story 1.3's AC 7, which
specified the summary as id / name / invitee count only.

Note: the negative colour used for the unanswered state is the dock's Carbon
`#CF6679`, because the Eggplant Wash identity is not implemented yet — it arrives
with Epic 3. Reconcile then.


## Entry 15 — Release control dropped; list title enlarged (2026-09-03)

At the user's direction:

- **The calendar list's title is three larger** than the row's own size
  (`calc(1em + 3px)`, so it tracks whatever the list uses rather than pinning a
  literal that would drift if the base changed).
- **The "I'm someone else" release control is removed** from the name dropdown.
  Switching is still possible — and is the real case — by picking a different name;
  what is gone is returning to *nobody*, which only made the app read-only for the
  person who chose it. `release()` stays in the composable: it is still how a claim
  naming a since-removed invitee gets cleared from storage. FR-10 updated in the
  spec and epics, and the EXPERIENCE.md open question about releasing is resolved
  rather than left hanging.


## Entry 16 — A past day opens; the claim prompt moves inside the sheet (2026-09-03)

Two corrections forced by building Epic 4 on top of Epic 3.

**Past days must be openable.** Story 3.5 made a past cell `disabled`, which read
as the right kind of "frozen" until Story 4.1 required an admin to mark a *past*
day chosen — unreachable through a disabled cell. The rule is now: a past day
cannot be **answered**, but can be **opened**. It is dimmed, its sheet says the day
has been and gone, and it still shows who was there — which turns out to be the
point of keeping past votes at all.

**The claim prompt moved into the sheet.** Story 3.2 had an unclaimed tap open the
name dropdown instead of the sheet. That made the sheet unreachable for an admin
who has not claimed a name — likely, since an admin need not be an invitee, so they
could never mark a day chosen. The sheet now always opens, and the "say who you
are" prompt sits inside it in place of the answer controls. The teaching moment
survives, one step later and in the context of the day just tapped.

FR-16 reworded in the spec and epics from "cannot be toggled" to "cannot be voted
on", and the EXPERIENCE.md component and interaction notes updated to match.


## Entry 17 — The wash reaches the list and the dialogs (2026-09-03)

Epic 3 themed the month and left everything around it on the dock's Carbon theme,
which the user asked to finish.

**Dialogs needed a second token root.** Quasar teleports `q-dialog` content to the
body, so a dialog card sits *outside* `.kdh-app` and inherited nothing — every
dialog dropped back to Carbon mid-flow. The tokens are now declared on
`.kdh-app, .kdh-panel`, and every KDH dialog card carries `kdh-panel`.

**Buttons revealed a real gap in DESIGN.md: KDH has no accent hue.** The spine
defines the wash and the invitee palette and stops, so the primary buttons were
still the dock's gold. Rather than invent an accent, a primary action is now a
raised surface (`wash-3` with `ink-hi`) and a destructive one is `danger` text on
transparent. That is the most faithful reading of "one idea carries colour" — a
coloured button would be a second thing competing with the only thing that
matters. Written into DESIGN.md under Components.

**`danger` is now genuinely a KDH token** rather than Carbon's negative borrowed by
accident, which is how entry 14 left it. The value is unchanged; the meaning is not.


## Entry 18 — The decided day goes gold; the count grows (2026-09-03)

At the user's direction:

- **The coverage count is one size larger** (8.5px → 10px). It is the backstop for
  two wash steps that look alike, so it was doing important work at a size chosen
  for decoration.
- **The chosen day's date is bold and gold**, with the diamond kept alongside.

Gold is a **third colour dimension** and was, until now, explicitly forbidden by
this spine ("never the dock's gold"). That rule was protecting against a second
thing competing for attention as an *interactive* accent — which gold still is
everywhere else on the dock. Used for "this is the decided day" it says something
the dock's gold never says, and it does not compete with the wash because it marks
one cell rather than ranking them. The Do/Don't is narrowed accordingly rather than
deleted.

Two golds, because one is not legible everywhere: `gold` on the dark end of the
ramp, `gold-deep` on `wash-5`/`wash-6` where the bright one washes out against pale
lilac — the same reason `ink-on-light` exists.

The diamond stays. The gold now carries the message, but NFR-6 says colour is never
the only signal, and someone who cannot separate gold from lilac still needs to see
which day was picked.


## Entry 19 — The decided day gets a ring and a glow (2026-09-03)

The first pass at gold was not loud enough. Brightened to `#FFD54A` and given a
glow, at the user's direction.

**The glow had to go on the cell, not only the date.** The chosen day is usually
the *full-coverage* day, which sits at `wash-6` — pale lilac — where bright gold
text has almost no contrast and a gold halo is mud. Text glow alone would have
failed on precisely the day it exists for. So the cell carries a gold ring and
outer glow, which reads at every step, and the text glow is an extra that only the
dark steps get.

Two smaller consequences, both invisible until they are wrong:

- **The chosen cell is lifted a layer.** Grid siblings paint in document order, so
  without a `z-index` the glow is covered by the background of every later cell in
  the month.
- **Chosen and provisional compose.** Both were single `box-shadow` rules, so one
  would have silently replaced the other on a day that is both. They are now one
  declaration — hairline inset, ring outside — with a test pinning that a day can
  wear both marks.


## Entry 20 — One gold everywhere, and bigger numerals (2026-09-03)

At the user's direction:

- **The date and the count each go up a size again** — 12.5px → 14px and
  10px → 11.5px. Both were originally sized as ornament on a cell whose whole job
  is to be read at arm's length.
- **The chosen day's date is the bright gold at every step of the ramp.** Entry 19
  had dropped it to `gold-deep` on `wash-5`/`wash-6`; the mark then looked like two
  different marks depending on how many people were free.

**This is a knowing accessibility trade.** Bright gold on pale lilac does not reach
4.5:1, which NFR-6 otherwise requires. Legibility comes from a dark halo behind the
glyph rather than from dulling the colour, and — the part that makes it acceptable
— the marking never rests on that text: the cell's gold ring, the diamond, and the
cell's accessible name each say the day is chosen independently. Recorded in
DESIGN.md's Do's and Don'ts as a named exception rather than left as a quiet
violation.

`gold-deep` survives for the diamond on the pale steps, where a bright gold shape
would disappear.


## Entry 21 — A crown replaces the diamond (2026-09-03)

At the user's direction, with the explicit requirement that the date stay aligned
with the rest of the month.

**The crown's slot is reserved in every cell, crowned or not.** Placing it in flow
only on chosen days would have pushed that one date down out of line with its
neighbours; positioning it absolutely would have overlapped the date in a 44px
cell. A fixed-height slot in all cells costs a few pixels of headroom everywhere
and guarantees the alignment. Pinned by a test.

**One crown, not two.** It is filled gold with a dark stroke, so it reads on the
pale top of the ramp as well as the dark bottom — the same approach the gold date
takes, rather than the darker variant the diamond used.

The crown remains the signal that does not depend on colour (NFR-6).


## Entry 22 — Hovering the count shows who voted (2026-09-03)

At the user's direction, with the mobile equivalent explicitly deferred by them.

The count is the one number in the cell that raises a question — *which* four? —
so it is the right thing to hang the answer on. The list is the day sheet's roster
content, in roster order, with if-needed people italicised rather than recoloured,
because their colour has to keep meaning *them*.

**Hover-only is a real limitation and is fine here.** KDH is phone-first, and a
phone has no hover, so on the target device this does nothing. It is still worth
having: the calendar is perfectly usable on a laptop, this is where a glance is
cheapest, and the same list is one tap away in the sheet regardless. A long-press
equivalent was not added — it would compete with the tap that opens the sheet,
which is the more important gesture. Logged as an open question rather than a
missing feature.

The cell's coverage number now has its own hook so tests can assert it without
picking up the floating list's text.


## Entry 23 — The count becomes a target, not just a number (2026-09-03)

The hover list from entry 22 hung off the digits themselves — about 7px wide, and
genuinely hard to hit. The count now sits in a padded, rounded target that
highlights on hover and anchors the list.

**Making room mattered more than the padding.** The cell had ~41px of content in a
~46px box, so the roomier target would have overflowed. The flex container's 2px
gap was removed and the spacing given to the pieces instead — the stack is now
~42px, leaving about 3.5px of breathing room, and the crown keeps a 1px margin so
it does not sit flush on the date.

Two smaller decisions:

- **The highlight darkens rather than lightens at the top of the ramp.** A white
  wash is invisible on pale lilac, the same reason `ink-on-light` exists.
- **Past days highlight too.** They cannot be voted on, but they can be read, and
  this is the read affordance — withholding it there would have been consistency
  for its own sake.


## Entry 24 — A web layout, and names return to the cell (2026-09-03)

At the user's direction. This reverses two decisions this session made
deliberately, and both reversals are right.

**NFR-0 was "phone-only, one layout".** It is now two: phone below 1024px, web
above. The phone layout is unchanged and is still the one the interface was
designed against — the web layout is the same app given room, not a different
design.

**FR-13 was "names are one tap away, not in the cell".** Entry 6 found that a
~44px cell cannot hold a name and moved them to the day sheet. That reasoning was
about *cell size*, not about phones, so on a wide layout — where a cell is roughly
150px — the original FR-13 becomes possible again and the names go back in,
trailing off past the sixth. The day sheet remains the complete list at both sizes.

Three consequences worth recording:

- **Wide cells are sized by the grid, not by an aspect ratio.** A ratio would
  either clip six names or leave a crater on an empty day; `grid-auto-rows:
  minmax(150px, auto)` keeps every row equal instead.
- **Names keep their owner's colour, with a dark halo on the pale top of the
  ramp**, where pastels have almost no contrast — the same fix the gold date uses.
- **The 80% band is capped at 1400px.** Uncapped, 80% of a 2560px monitor is a row
  of billboards rather than something you read. The cap is an addition to the
  instruction, and easy to remove.


## Entry 25 — The web layout, tightened (2026-09-03)

Three corrections after seeing entry 24 on a real screen:

- **The band narrows from 80% to 70%.**
- **The voter names become one comma-separated line** at the foot of the cell,
  in the cell's own ink rather than each person's colour. Six colours on one line
  is a smear, and the colour still does its work where it can be read one name at
  a time — the day sheet and the hover list. The if-needed distinction stays as
  italics, because NFR-6 asks for a signal that is not colour, and italics cost
  nothing here.
- **The hover target grows substantially, on web only.** Hover does not exist on a
  phone, and the phone cell has about 3px of spare height, so enlarging it there
  would have cost layout for no gain.


## Entry 26 — Square cells, a smaller band, a louder header (2026-09-03)

At the user's direction:

- **The chosen day loses its dark outline** on both the date and the crown, and
  gains a gold glow instead. **This costs real contrast on `wash-5`/`wash-6`**,
  where the mark is now gold on pale lilac with nothing dark to separate it. It is
  acceptable only because the marking never rested on the number: the cell's gold
  ring, the crown and the accessible name each say "chosen" independently. The
  Do/Don't in DESIGN.md is updated to say so plainly rather than still claiming a
  halo carries it.
- **The band narrows again, 70% → 58%** (cap 1080px).
- **The calendar's name and headcount are much larger** — 22px/19px on a phone,
  34px/26px on web.
- **The identity control moves to the right of the header**, beside the admin menu.
  It is a control rather than part of the title, and the bigger title needs the
  width.
- **Day cells are square at both sizes.**

**Square plus a narrower band is a genuinely tight budget.** At a 1440px window a
web cell is ~110px and the contents come to ~105px. At 1280px the cell is ~96px, so
the names block shrinks from two lines to one — deliberately, via `flex: 0 1 auto`
and a max-height, so it clips at a line boundary instead of spilling past the
square. The day sheet remains the complete list, so nothing is lost, only deferred
to a tap.


## Entry 27 — Marks move off the cell and into the gutter (2026-09-03, Sally)

Several rounds of gold, glow and ring had made the chosen day loud without making
it good. The diagnosis, and the reason nothing was working: **the cell interior was
already fully spoken for** — its background is the ordered wash (*how many*), its
text is categorical (*who*) — so every marking was competing for a surface with no
room left. But *chosen*, *today* and *weekend* are not facts about coverage; they
are facts about the day, and they belong in the channels the wash does not use: the
gutter, the frame, elevation.

Three directions were rendered on the same month (`mockups/marking-days.html`) and
the user chose:

- **Chosen — a gold frame in the gutter.** The only mark that looks identical at
  `wash-0` and `wash-6`, because it never touches the wash. Sized to the gap it
  sits in: 2px on a phone, 2.5px on web.
- **Today — unchanged**, the existing inset outline.
- **Weekend — a faint band painted behind the last two columns**, showing only in
  the gutters and the grid's padding.

**The frame solved the legibility problem by subtraction.** Once the frame carries
the message the number no longer has to, so the glow and the dark outline are both
gone, and the date on the pale steps simply drops to `gold-deep` and is readable.
Entry 26's knowing contrast exception is therefore **withdrawn** — there is nothing
left to excuse — and `--kdh-gold-glow` is deleted rather than left as a dead token.

Recorded as a rule in DESIGN.md: mark a day in a channel the wash does not use, and
make marks differ **in kind** rather than in colour.


## Entry 28 — The names sit under the count, not at the foot (2026-09-03)

The names were anchored to the bottom of the cell with `margin-top: auto`. In a
square cell whose contents already come to within a few pixels of its height, that
pinned them against the bottom edge and they spilled out of it.

They now follow the count directly, with a small margin. Spare height, when there is
any, collects at the foot of the cell instead — which is also what the user asked
for visually. A few pixels were reclaimed from the crown slot and the count's
padding at the same time, so the square holds its contents from about 1360px
upward; below that the names block shrinks toward one line rather than overflowing.


## Entry 29 — The band widens, with a floor (2026-09-03)

58% → 66%, and — the part that actually fixes it — a **790px floor**. The names
were disappearing on narrow windows because a percentage band shrinks with the
window while the cell's contents do not: below about 790px the square cells stop
being tall enough for two lines. A minimum width means a narrow window gets a wider
*proportion* rather than losing the names.

Cells now clear the ~101px they need at every width from 1024px (the breakpoint)
upward, with headroom growing to the 1080px cap.


## Entry 30 — The breakpoint moves to 700px, and the web type goes fluid (2026-09-03)

Moving the breakpoint alone would have broken the low end. Seven square cells across
a 700px window are ~85px each, and the web layout's fixed numerals came to ~101px of
content — the names would have been pushed straight back out of the cell, which is
the bug fixed two commits ago. The 790px band floor would also have overflowed a
700px window.

So the web layout's type is now **fluid**: the crown, the date, the count and the
names all `clamp()` between a size that fits an 85px cell and the size chosen for a
comfortable window, scaling with the viewport instead of jumping at a second
breakpoint. The band's floor is `min(790px, 100%)`, so it can never exceed the
window.

This keeps the "two sizes is enough" rule while making the larger of the two elastic
— which is what it always needed to be, given it now spans 700px to 2560px.


## Entry 31 — Truncated names now say they are truncated (2026-09-03)

The names block was limited by `max-height`, which crops in silence — a day with
long names simply ended mid-word and nothing said there was more. It is now a
two-line `line-clamp`, so the browser truncates at a line boundary and marks it with
an ellipsis.

There are now two ellipses in the design and they mean different things: the
explicit `…` appended past the sixth name means *more people are on this day*, while
the clamp's means *more text than fits*. They rarely both appear, and when they do
only the clamp's is visible. Both point at the same escape hatch — the hover list on
web, the day sheet everywhere.


## Entry 32 — Invitee colours were nearly cut; they earn their place instead (2026-09-03, Sally)

The user asked whether invitee colours add anything. The audit said: almost nothing.
Every render site — day sheet, hover list, name dropdown, roster chips, the claimed
pill — put the colour **beside the name it identifies**, where the name had already
done the job. A colour that only ever appears next to its own label is decoration,
and it was costing a field on every invitee, an eight-hue palette, an endpoint, a
swatch UI and a rule about staying out of the purple family.

The one job only colour can do had never been used: the **phone day cell has no
names at all**, just a date and a count. It now carries a row of dots, filled for
*free* and hollow for *if needed*, in roster order and capped at six — the count
carries the true total.

**The phone cell grew rather than its contents shrinking** — `1 / 1.06` to
`1 / 1.28`. The date and the count are at their sizes because they were deliberately
enlarged twice; taking that back to fit dots would have been robbing one decision to
pay for another.

Recorded in DESIGN.md as a rule: an invitee colour must earn its place by
identifying someone whose name is not already there. Everything else is convenience.


## Entry 33 — Invitee colours removed entirely (2026-09-03)

Entry 32 kept the colours by giving them a job: dots in the phone cell. Seeing it,
the user did not like the dots and asked for both to go.

That is the right end of the trade. The audit in entry 32 already found that every
*other* appearance was a dot beside the name it identified — decoration, not
information — and the dots were the only thing keeping the whole apparatus alive.
Without them there is nothing left for a colour to say.

Deleted: `Invitee.color`, `PALETTE`, `assign_colour`, `recolour_invitee`,
`PUT /invitees/{id}/color`, `INVITEE_PALETTE`, `recolourInvitee`, the swatch UI,
every dot, and the eight-hue block in `DESIGN.md`. **FR-9 is withdrawn** and Story
2.4 is marked superseded rather than deleted, so the epic's numbering still tells
the truth about what was built and then removed.

Two consequences worth naming:

- **The roster is no longer capped at eight.** The cap was never a product rule; it
  was the palette running out. A calendar can now hold as many people as you like,
  and a test pins that.
- **No migration is needed.** Pydantic ignores unknown fields, so calendars already
  on disk with a `color` on each invitee load unchanged and simply drop it on the
  next write. Verified rather than assumed.

The rule that survives, in `DESIGN.md`: a colour must earn its place by saying
something no other element on screen is already saying. The wash does. Nothing else
did.


## Entry 34 — The day sheet, redesigned (2026-09-03, Sally)

Two problems, one cosmetic and one not.

**Free and if-needed differed only in italics and a trailing caveat.** Adequate
while every person also had a colour; the faintest signal in the app once the
colours went. They now differ in **shape** — a filled glyph against a half-filled
one — with the italics kept as reinforcement rather than as the whole distinction.

**The sheet answered "who is coming" but not "how good is this day".** It now opens
with a proportional bar: free, if-needed hatched in the grid's own language, and the
remainder. Three directions were rendered (`mockups/day-sheet.html`); the user chose
this one.

Two decisions taken deliberately:

- **Silence is a no.** The design showed a "still to answer" group and the user cut
  it: people who have not answered are counted in the remainder, not listed. That
  keeps the sheet a list of who is coming, and — the reason it matters — means the
  app never has to distinguish *declined* from *never looked*, which would have
  needed a third stored status and a migration.
- **Green and yellow appear on the answer control only.** Everyday language on the
  one element the user is about to press; the rows stay in the wash's lilac. A
  deliberate asymmetry, worth revisiting if it reads as inconsistent rather than as
  control-versus-data.

A test caught a real bug on the way: `:style="{ flex: 0 }"` is dropped by Vue,
because `0` is falsy — so on a fully-covered day the "not coming" segment kept its
default width and the bar showed a gap that was not there. Set as an explicit
`flexGrow` string instead.


## Entry 35 — Bulk answers and day notes (2026-09-03)

**Bulk.** A selection mode toggled from the month header — a tap then collects a day
instead of opening it. Deliberately a mode rather than a drag: a drag across a grid
of tap targets is too easily started by accident, the same reason the month has no
swipe. Past days cannot be collected, since the server would refuse them anyway. The
write is **one transaction, all-or-nothing**: a selection containing one bad day
leaves the calendar untouched rather than half-applied, and one lock rather than N
matters when this is how someone answers a whole month.

Not optimistic, unlike a single vote. A single tap has to move under the thumb; this
is one deliberate action where a moment of waiting is honest, and the all-or-nothing
guarantee means a half-updated grid is not a state that can exist.

**Notes.** Independent of an answer, which is the whole point — the useful note is
usually *why you cannot come*. The day sheet therefore lists anyone with an answer
**or** a note, and a note-only person shows a hollow glyph and a NOTE ONLY tag. Notes
never move the counts: the bar stays vote-only, so saying something is never mistaken
for saying yes.

Reaching a note: `q-tooltip` for hover and `q-menu` for tap, on the same pip. Not
device detection — a touch screen never fires hover and a pointer rarely needs the
menu, so both coexist without either being conditional.

No ownership on notes, at the user's direction. Everyone shares one login, so the
claim was never a boundary and enforcing one here would have been theatre.


## Entry 36 — Past days stop opening (2026-09-03)

At the user's direction, with the hover list explicitly to be preserved.

**Inert, not `disabled`.** A disabled button suppresses pointer events on its
children in every browser, which would have taken the hover list of who was there
with it — and that list is the only reason a past day is still worth looking at. So
the click handler returns early instead, and the cursor stops suggesting the cell is
pressable.

**This costs a capability, deliberately rather than by oversight.** Marking a past
day chosen (Story 4.1, and the reason entry 16 made past days openable at all) was
reachable *only* through the day sheet. With past days no longer opening, an admin
can no longer record that a past session happened. Restoring it is one condition on
the guard in `DayCell` — `if (props.past && !isAdmin) return` — and a test now
documents the current behaviour, so the choice stays visible rather than silent.


## Entry 37 — "Select Multiple" becomes a button (2026-09-03)

The way into bulk answering was a flat icon among the month's navigation
controls, which made the invitee's main action look like furniture. It is now a
filled `wash-4` pill with the label, sitting in the month header.

It disappears once selecting has begun: leaving is the Cancel on the selection bar,
and two exits from one mode is one too many. The month label takes the remaining
width and ellipsises, so a long month name cannot push the button off a narrow
screen.

Worth noting against the open question in entry/artifact "one tap, two jobs": this
makes the *current* model — a mode you enter deliberately — as clear as it can be,
which is useful either way. If the "selection is the app" model wins, this button
disappears entirely rather than being restyled again.


## Entry 38 — Your own answer, in the leading corner (2026-09-03)

**Problem.** Below 700px the cell carries no names, so there was nothing in the
month telling you which days *you* had answered — the one thing an invitee opens
the app to check.

**Why a glyph.** Filled means free and half-filled means if needed on the answer
buttons and on every roster row in the day sheet. Reusing that vocabulary here
costs nothing to learn; a new mark would have.

**Why the leading corner of the crown row, and not the two obvious alternatives.**
Everything else in a cell is a *group* fact: the wash is how many can come, the
pill counts them, the crown is the decision. Your answer is the only *personal*
fact, and a personal fact put among group ones gets read as a group one.

- *Inside the count pill* (`● 4`) was the cheapest — no layout cost at all — and
  is exactly the trap: at 3px apart the eye binds the glyph to the number and
  reads "4 free", or "● and 4 others". A private fact inside a public tally.
- *Left of the date* is the most visible, and breaks the alignment the reserved
  crown slot exists to protect.

The mark pins to the leading edge of the reserved row while the crown stays
centred, so they share the row and never the spot. Because it is in the same
place in every cell, the month becomes scannable: you can read your own answered
days as a pattern in the left margin without reading a number anywhere.

**Colour.** Green and yellow, the same two the answer buttons use — "your own
answer" is now consistently green/yellow throughout, against a world of lilac and
gold that is consistently about the group. They also read at every step of the
ramp, which the roster's lilac would not at the pale top. Half versus whole
carries it without colour (NFR-6).

**Consequence, accepted not solved.** A "can't" is stored as no vote, so a
deliberate no and a never-opened day are the same absence. The mark therefore
means *days I said yes to*, not *days I answered*, and copy near it must match.
Giving a deliberate no its own mark needs a data-model change and is a separate
question.

**Also.** The select-mode tick moved to the trailing corner; the leading one is
spoken for now.


## Entry 39 — The mark drifted to the middle on a phone (2026-09-04)

Reported: correct on web, floating near the centre of the cell on a phone.

The mark had been hung off the crown slot, on the reasoning that the slot is the
row it shares with the crown. But the slot is a centred flex row that carries no
in-flow content on an uncrowned day — when its `width: 100%` does not resolve it
collapses to zero width, the cell's `align-items: center` centres that empty box,
and a mark positioned 3px from its leading edge lands 3px right of the cell's
centre. Which is precisely what was on screen.

It is anchored to the cell's own box now. The cell was already positioned, is
always full size, and cannot drift. The crown slot went back to what it was; it
was never holding space for the mark anyway, since an absolutely positioned mark
occupies none.

Design intent is unchanged — leading corner, opposite the select tick. Only what
it measures from changed. A structural test now asserts the mark is a child of
the cell rather than of the slot, because that relationship is the fix.

**Found alongside:** the web `.crown-slot` height override had never applied. It
sits above the base rule at equal specificity, so the base won on source order and
pinned the slot to its phone height while the crown inside it scaled with the
viewport. Given the specificity to win. The mark's own web override had the same
defect; it is now one `clamp()` in the base rule instead, which cannot be ordered
wrong.


## Entry 40 — Bulk answers all three, and overwrites (2026-09-04)

The selection bar offered one verb, "Mark free". It now offers the day sheet's
three answers verbatim — same words, same glyphs, same order — because this is
the sheet's question asked of many days at once, and asking it differently would
imply it is a different question.

**Overwrite, not fill-in-the-blanks.** Applying an answer to a selection replaces
whatever each day held: a day already marked *if needed* becomes whatever you just
picked, and *Can't* clears the answer outright. Bulk is the same act as answering
in the sheet, so it settles a day rather than tiptoeing around days you had
already touched. The backend already worked this way; only the UI had been
withholding two of the three answers.

**Layout.** Three answers plus Cancel do not fit across a phone, so the bar became
two rows: the count and Cancel above, the three answers below as equal thirds.
Equal widths on purpose — no answer should look like the default one, which is
exactly what a single "Mark free" button had made *free* into.


## Entry 41 — The name pill joins the purple family (2026-09-04)

The claimed name pill was `field-raise` — near-black, indistinguishable from the
field it sat on, and the only header control with no colour at all. It is
`{wash-3}` now, with a `{wash-4}` hairline.

**One step below "Select Multiple", deliberately.** That button is `{wash-4}`
because it is a call to act; the name pill is a standing statement of who you are,
and EXPERIENCE.md asks a *claimed* pill to settle rather than compete. Two adjacent
steps of the same ramp read as two ranks of one family — which is the true
relationship — where two unrelated purples would have read as two unrelated
controls. Hovering lifts it to `{wash-4}`, so it meets the button's rank only
while you are actually pointing at it.

The unclaimed state is untouched: it stays `{danger}`, still the loudest thing on
the page, because nothing else can be done until it is answered.

**Note on the ramp.** The theme's opening comment calls the wash "the only purple
in the interface", meaning the coverage scale owns purple *inside the grid*. Chrome
outside the grid has been borrowing its steps since the selection bar shipped
(`sel-btn.go` at wash-3). Worth a deliberate decision later about whether the ramp
is a coverage scale or a general elevation scale — it is currently both, and the
comment no longer describes the code.


## Entry 42 — Clear a month of your own answers (2026-09-04)

Added to the header menu: **"Clear my answers for {Month}"**, acting on the month
currently on screen.

**The menu stops being admin-only.** It held nothing but admin actions, so it was
hidden from guests entirely — but this action belongs to whoever is looking, and
guests are the people who actually answer. The button now appears for anyone with
a claim, labelled *"Your answers"* rather than *"Calendar actions"* when the
person is not an admin. A guest opens a one-item menu.

The FR-3 invariant is unchanged in substance: admin markup must not merely be
unreachable for a guest, it must not exist. What moved is where that is enforced —
from one `v-if` around the whole group of dialogs to a `v-if` on each admin item.
A guest still receives no rename, share, invitee or delete markup.

**Future dates only, and only yours.** Past answers are records: the server
refuses to rewrite one, and "clear this month" has never meant "erase history" to
anyone. Days you never answered are excluded too, so the number in the
confirmation is the number of answers that will actually disappear — not the
number of days in the month. Nobody else's answers are touched.

**It asks first,** unlike bulk answering from the selection bar. The asymmetry is
deliberate: collecting days by hand is an explicit statement of intent, where one
tap in a menu can undo a month of answering. The confirmation names the month and
the count, and says past days are safe.

**Notes are left alone.** Clearing a vote does not clear the note you left with
it — a note explaining why you cannot come outlives the answer, and the day sheet
already lists a note-only person. Worth revisiting if it surprises anyone.

**Implementation note.** No new endpoint: it is `set_votes_bulk` with status
`none`, which already validates every day as future and writes in one transaction.
MonthGrid now exposes `dates` alongside `monthLabel` so the page can see what is
on screen.


## Entry 43 — Clearing a month takes the notes with it (2026-09-04)

Three corrections to entry 42.

**Notes go too.** Entry 42 kept them, reasoning that a note outlives the vote it
came with. That holds for answering *Can't* — "away that week" belongs beside a no,
not deleted by it — but not for clearing a month, which is a person erasing their
mark on it. Leaving the notes made "clear" a lie.

So the two cases are now separated explicitly rather than sharing one default:
`clear_notes` on the bulk request is **off** unless asked for, the selection bar
never asks, and the month sweep always does. A day carrying only a note and no
vote is now swept as well, and counts toward the number in the confirmation.
Nobody else's notes are touched.

**"Votes", not "answers".** The menu item, the dialog and the non-admin
`aria-label` all say votes now. The day sheet still *asks* ("Can you make it?") and
still names the three states in plain words — that is the moment of answering.
Away from that moment, what you left behind is a vote, and the app should call the
same thing by one name.

**The confirmation is two lines.** What goes and what is safe are separate
sentences on separate rows, the reassurance quieter than the warning. Trailing
"days already past are left alone" on the end of the same sentence buried the one
line that stops a person hesitating.

Snagged on the way: the reassurance line first used `.kdh-muted`, which is defined
inside DaySheet's *scoped* styles and would have silently done nothing on this
page. It has its own class now.


## Entry 44 — A tally under the month (2026-09-04)

A per-person summary sits beneath the grid: how many days each invitee has voted
on in the month on screen, split into still-to-come and past.

**Scoped to the month, confirmed with the user.** It sits under the month and
moves with the arrows. The alternative — a lifetime total per person — answers
nothing anyone asks; the month answers "who still has not said anything", which is
the one question the grid cannot show at a glance. You can see *that* a day is
thin, but not *whose* silence is making it thin.

**Today counts as still to come**, as it does everywhere else in KDH: a day you
can still answer is not yet history.

**The zero half is dropped.** "3 to come" rather than "3 to come · 0 past" — the
absent half carries no information, and a row of zeroes reads as data when it is
only padding.

**Silence is quieter, not louder.** A person who has not voted reads "nothing yet"
in `{ink-lo}`. Nagging is not this app's job, and the gap in the column is enough
to see without the summary shouting about it.

**Before anyone has voted it collapses to one sentence.** A column of "nothing yet"
against an empty grid tells you nothing you cannot already see; the per-person rows
earn their place only once there is a difference between people to notice.

**Someone who has left appears only where they voted this month**, tagged LEFT —
their past votes are a record the grid already shows, but an empty row for a person
no longer in the group is noise.


## Entry 45 — Total first, breakdown after (2026-09-04)

The tally led with the split — "2 current · 2 past" — and never stated the number
a person actually asks for. It reads `11 (10 current · 1 past)` now: the total in
`{ink-hi}` and bold, the breakdown smaller and quieter behind it, explaining the
total rather than competing with it.

**The shape is the same on every row**, including when one half is all of it:
`3 (3 current)`, not `3 current`. That is mildly redundant on its own, but it puts
the total in the same place on every line, and a column read straight down is the
whole point of the thing. A zero half is still never named.

"Current" rather than "to come", the user's own word.


## Entry 46 — The tally becomes a ranked bar chart (2026-09-04)

Three changes, arriving together and pulling in the same direction.

**The bracket earns its place or goes.** Entry 45's `11 (10 current · 1 past)`
stated both halves everywhere. But only the month containing today HAS two halves:
a month gone by is entirely past, a month ahead entirely not, so outside the
current month the bracket could only restate the total. It now reads `11 (1 past)`,
and only in the current month, and only when something is actually past.

**A bar per person, scaled to the busiest.** Not to the days in the month: the
question is who has answered and who has not, and a bar 11/30 full of everything
answers it worse than one 11/11 of the most anyone managed. The bar splits current
from past in the same two shades the text uses — `{wash-4}` spent down to
`{wash-2}` — and someone who has said nothing gets an **empty track**, not a
missing one. The empty track is the point: a gap you can see down the column.

Rows are a three-track grid so every bar starts at the same x and shares one
scale. A chart whose bars start in different places is not a chart.

**Ordered by count, descending.** This is what turns the three into one thing: a
roster with bars beside it is decoration, but a list sorted by the bar is a chart
you read top to bottom, and whoever has said nothing sinks to the bottom where the
gap is easiest to see. Roster order breaks ties so equal counts never shuffle
between renders.

The bars are `aria-hidden`. The number sits immediately beside each one, so a
screen reader gains nothing from the shape and loses nothing without it.


## Entry 47 — The bars did not start at the same x (2026-09-04)

Reported straight after entry 46: the chart's bars were out of line.

Entry 46 put the three tracks on `.tally-row`, which made **every row its own
grid**. The counts track was `auto`, so it sized to that row's own content — a
"11 (1 past)" row reserved more width than a bare "8", the `1fr` name column
absorbed the difference, and each row's bar started somewhere slightly different.
Bars that start in different places are not a chart; they are three unrelated
bars.

The grid moved up to a `.rows` container that holds the whole list, and the rows
became `display: contents` so their cells land in the shared tracks. One
declaration, one scale, one starting x.

`display: contents` costs the row its own box, so the per-row padding became
`row-gap` on the container. The row div survives as a class carrier — `.mine` and
`.silent` still reach their descendants, and `data-testid` still resolves.

The test asserts the structure rather than the geometry, since layout is not
computed in the test environment: one grid container, every row inside it.


## Entry 48 — The invitee link replaces the shared account (2026-09-04)

The group reaches KDH through a **public link per calendar** now, not a shared
login. This reverses the decision taken early on ("Both — account now, link
later") and pulls the deferred epic forward.

**The token is the whole authorisation.** 192 bits from `secrets.token_urlsafe`,
one per calendar, minted at creation and lazily for calendars made before the
feature existed — minted once and persisted, because a token generated fresh on
each read would hand out a different link every page load and break every link
already pasted into the chat.

**The admin surface is ABSENT from the share router, not guarded inside it.**
`share_router` carries four routes: read the calendar, vote, bulk-vote, note.
Rename, delete, roster and chosen days are not written there at all. A route that
does not exist cannot be reached through a bug in a guard, so the shape of the
file is the security argument, and a test asserts that shape rather than the
behaviour of a check.

**What an invitee does not see, and why each:**

| Hidden | Reason |
|---|---|
| Back button | The list needs a login they do not have; a button to a login screen is a dead end |
| Rename / invitees / delete | Running the calendar is not theirs to do |
| Copy link | They already hold it, and re-sharing is the organiser's decision |
| Chosen day | Deciding is the organiser's act; the crown is a broadcast, not a vote |
| Month tally | A view of how the *group* is doing — the organiser's question — and it names every person's silence to anyone holding the link |

**They keep the ⋮ menu**, because "clear my votes for {Month}" is theirs. A guest
opens a one-item menu, which is the shape entry 42 already built.

**"Copy link" now copies the invitee link.** It used to copy the address bar,
which is the admin route: pasting that into the group chat sent everyone to a
login screen. Renamed "Copy invitee link" so the menu says which of the two it is.

**A dead link and a deleted calendar read identically** — "This link no longer
works. Ask whoever shared it for a new one." Telling them apart would let anyone
probe for which tokens are real. The API answers the same way: a bare 404 with no
detail.

**Naming.** The user asked for a "read-only link". It is read-only with respect to
*running* the calendar — voting, noting and clearing are the point of it — so the
code and copy call it the **invitee link**, and never "read-only", which would
promise something it does not do.

**Open, not actioned:** the `players` guest account and the `kdh_guests` denylist
are now redundant. They still work and still gate the authenticated routes, so
nothing is broken by leaving them; removing them is a separate decision. There is
also no way to revoke a leaked link — regenerating a token would need one more
admin action, and nobody has asked for it yet.


## Entry 49 — The link is reachable from the list (2026-09-04)

A link icon on each row of the calendar list copies that calendar's invitee link.
Handing the link out is the most common thing an organiser does, and it took
opening the calendar and finding a menu item.

`CalendarSummary` carries `share_token` now. Safe here and nowhere else: that
schema is only ever served to a logged-in caller, and the public routes never
return a summary. The alternative — fetching the calendar on click — would have
put a request between the tap and the clipboard, which is the one place a delay is
felt.

A calendar made before links existed is minted one during the listing, so a row's
button never does nothing. A newly created calendar carries its token into the
locally-added row, so the button works before any refetch.

`@click.stop`, because the row itself navigates and copying is emphatically not
"open this" — there is a test for exactly that.

One notice for the whole list rather than one per row: the clipboard is
unavailable on plain HTTP over a LAN, which is how this is actually run, and the
fallback is a full URL that no row is wide enough to hold.

Hidden from a guest, matching the menu item it duplicates.
