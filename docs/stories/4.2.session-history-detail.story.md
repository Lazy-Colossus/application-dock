# Story 4.2: Session History Detail

## Status

Ready for Review

## Story

**As an** operator,
**I want** to tap a past session and see the full ranked results and per-target breakdown,
**so that** I can review any previous round in detail.

## Acceptance Criteria

1. **Given** the operator taps a session entry in the history list, **When** the navigation completes, **Then** `HistoryDetailPage.vue` renders the full results for that session using the same `ResultsTable` component as the live results screen.
2. **Given** the history detail view, **When** it renders, **Then** it shows each archer's total score ranked highest to lowest, and a scrollable per-archer per-target breakdown (target number, shot 1, shot 2, subtotal) — identical layout to the live results screen.
3. **Given** the `GET /api/archery/sessions/{label}` endpoint, **When** called with a valid session label, **Then** it returns the full `SessionData` object for that session; `session_repo.read_session(label)` handles the file lookup.
4. **Given** the `GET /api/archery/sessions/{label}` endpoint, **When** called with a label that does not match any file, **Then** it returns 404; the frontend shows an error state.
5. **Given** the history detail view, **When** the operator taps the back button or home control, **Then** no edit controls are present — the view is strictly read-only; no "Finalise" or "Return to Scoring" actions appear.
6. **Given** sessions are finalised, **When** the operator is in the history detail view, **Then** there is no delete or edit option — history is append-only (FR-2.21).

## Tasks / Subtasks

- [ ] **Task 1 — Backend service: `read_finalised(label)`** (AC: 3, 4)
  - [ ] In `backend/app/services/archery_service.py`, add:
    ```python
    def read_finalised(label: str) -> SessionData:
        s = session_repo.read_session(label)  # raises FileNotFoundError if absent
        if s.status != "finalised":
            # Defensive: don't expose the in-progress file via this endpoint
            raise FileNotFoundError(label)
        return s
    ```
  - [ ] Validation of `label` against path traversal: reject if `label` contains `/`, `\\`, or `..`. Pydantic `Path(..., regex=r"^\d{4}-\d{2}-\d{2}(-\d+)?$")` is the cleanest enforcement (in the router signature) — only allow `YYYY-MM-DD` or `YYYY-MM-DD-N` labels [Source: security — never trust path params from URLs, even no-auth, LAN-only]

- [ ] **Task 2 — Backend router: `GET /api/archery/sessions/{label}`** (AC: 3, 4)
  - [ ] In `backend/app/routers/archery.py`:
    ```python
    from fastapi import Path

    LABEL_PATTERN = r"^\d{4}-\d{2}-\d{2}(-\d+)?$"

    @router.get("/sessions/{label}", response_model=SessionData)
    def get_session(label: str = Path(..., pattern=LABEL_PATTERN)):
        try:
            return archery_service.read_finalised(label)
        except FileNotFoundError:
            raise HTTPException(404, detail=f"Session {label} not found.")
    ```
  - [ ] Note: this route is registered AFTER `/sessions/in-progress` (Story 3.2) — FastAPI's routing will match the literal `/in-progress` first because it's registered first and is a more specific path. **Verify route registration order** in `archery.py`: `/sessions` (list, 4.1) → `/sessions/in-progress` (GET/PUT/DELETE) → `/sessions/in-progress/finalise` (POST) → `/sessions/{label}` (this story). The Pydantic regex on `{label}` ALSO defends against accidental matches.

- [ ] **Task 3 — Frontend: extend `useArcheryHistoryStore` with `loadDetail`** (AC: 1, 4)
  - [ ] In `useArcheryHistoryStore.ts`, add:
    ```ts
    const detail = ref<SessionData | null>(null)
    const detailLoading = ref(false)
    const detailError = ref<string | null>(null)

    async function loadDetail(label: string): Promise<void> {
      detail.value = null
      detailLoading.value = true
      detailError.value = null
      try {
        detail.value = await api.get<SessionData>(`/archery/sessions/${encodeURIComponent(label)}`)
      } catch (e: any) {
        if (e?.status === 404) {
          detailError.value = `Session ${label} not found.`
        } else {
          detailError.value = e instanceof Error ? e.message : String(e)
        }
      } finally {
        detailLoading.value = false
      }
    }
    ```
  - [ ] Keep `summaries`, `loading`, `error` separate from detail state to avoid clobbering the list when navigating

- [ ] **Task 4 — `HistoryDetailPage.vue`** (AC: 1, 2, 5, 6)
  - [ ] Create `frontend/src/apps/archery/pages/HistoryDetailPage.vue`
  - [ ] Reads `label` from `useRoute().params.label`
  - [ ] On mount: call `historyStore.loadDetail(label as string)`
  - [ ] Page title (route meta): "Session" (or dynamic — bind to `detail.label` via `meta.title` reactive update)
  - [ ] Layout (single column, 16px padding):
    - While `detailLoading`: centered `QSpinner` (`accent` color)
    - If `detailError`: `QBanner` at top with the error message + a Back button → `router.push('/archery/history')`
    - Otherwise:
      - Session label heading at top (`display` typography, `ink-primary`)
      - Ranked summary section (same as Story 2.5 `ResultsPage`): one card per archer, sorted by total descending, with rank chip, name, total. Use the ranking helper from Story 2.5 (`useScores.ts` composable).
      - `<ResultsTable :session="detail" />` — the SAME component as live results (Story 2.5) [AC 1, 2]
  - [ ] **NO action buttons.** No "Finalise". No "Return to Scoring". No "Delete". No "Edit". The page is strictly read-only. [AC 5, 6, FR-2.21]
  - [ ] Back navigation: rely on `MainLayout.vue`'s top app bar back arrow (Story 1.2) — it triggers `router.back()` automatically

- [ ] **Task 5 — Router additions**
  - [ ] In `frontend/src/router/index.ts` add:
    ```ts
    { path: '/archery/history/:label', component: () => import('@/apps/archery/pages/HistoryDetailPage.vue'), meta: { title: 'Session' } }
    ```
  - [ ] `label` will be URL-encoded by the navigator; decode in the page via `decodeURIComponent(route.params.label as string)` (or use the encoded value directly — `useRoute().params.label` returns the decoded value by default in Vue Router 4)

- [ ] **Task 6 — Wiring from history list** (AC: 1)
  - [ ] Confirm Story 4.1's `HistoryPage.vue` already navigates to `/archery/history/${encodeURIComponent(summary.label)}` on row tap; this story does NOT change that page.

- [ ] **Task 7 — Tests**
  - [ ] Backend: `backend/tests/test_archery_get_session.py`:
    - Seed `2026-05-21.json` with a finalised `SessionData` → GET `/api/archery/sessions/2026-05-21` → 200, body matches
    - GET `/api/archery/sessions/2026-05-99` (invalid date in regex range — but regex accepts) → 404 (file does not exist)
    - GET `/api/archery/sessions/foo` → 422 (path regex rejects)
    - GET `/api/archery/sessions/../etc/passwd` → 422 (regex rejects) — verifies traversal defense
    - GET `/api/archery/sessions/_in_progress` → 422 (regex rejects underscore prefix)
    - Defensive: seed a file `2026-05-21.json` with `status: "in_progress"` → GET → 404 (don't expose in-progress via this route)
  - [ ] Frontend: `HistoryDetailPage.spec.ts`:
    - Mount with route param `label=2026-05-21`, mock store to return a finalised session → renders `ResultsTable` and ranked summary
    - Mock store with 404 error → renders error banner with back button
    - Verify NO Finalise / Return-to-Scoring / Delete / Edit buttons are present (use `wrapper.text()` / DOM query)
  - [ ] Frontend: `useArcheryHistoryStore.spec.ts` (extend):
    - `loadDetail` happy path → `detail` populated, `detailLoading` toggles
    - `loadDetail` with 404 → `detailError` set, `detail` is `null`

## Dev Notes

### Previous Story Insights
- **Story 2.1** built `session_repo.read_session(label)` (returns `SessionData`, raises `FileNotFoundError`). Reuse; do not duplicate.
- **Story 2.5** built `ResultsTable.vue` and (optionally) `useScores.ts` ranking composable. **This story consumes them unchanged.** The shared component is critical for AC 1, 2 ("identical layout to the live results screen").
- **Story 4.1** built `useArcheryHistoryStore` (with `loadHistory`); this story extends it with `loadDetail`. Both stores share the same Pinia instance — keep state cleanly separated (list state vs. detail state).
- **Story 1.2** built `MainLayout.vue` with the persistent back arrow + home button. The history detail page relies on these; no page-specific navigation chrome is needed.

### Source references
- Architecture: `_bmad-output/planning-artifacts/architecture.md`
- UX behavior: `_bmad-output/planning-artifacts/ux-designs/ux-Code-2026-05-28/EXPERIENCE.md` (Key Flow 3 — Reviewing last week's session)
- Epic: `_bmad-output/planning-artifacts/epics.md` (Story 4.2 — FR-2.20, FR-2.21)

### API contract — `GET /api/archery/sessions/{label}`
[Source: architecture.md#API & Communication Patterns]

- **Path param `label`:** regex `^\d{4}-\d{2}-\d{2}(-\d+)?$` (enforced by FastAPI `Path(..., pattern=...)`)
- **Success (200):** Full `SessionData` body with `status: "finalised"`
- **404:** Label valid format but file not found, OR file exists but has `status: "in_progress"` (defensive — only this route's finalise-detail responsibility; the in-progress route is separate)
- **422:** Label format invalid (e.g. `"foo"`, `"../etc"`, leading underscore) — FastAPI default validation response
- **No envelope.**

### Security note (path traversal defense)
[Not explicitly in architecture.md; standard practice for LAN-but-still-good-hygiene]

- The regex on `label` blocks `..`, `/`, `\\`, leading underscores, and any non-conforming string
- `session_repo.read_session()` constructs the path with `settings.data_dir / f"{label}.json"` — combined with the regex, traversal is not possible
- Even though the app is LAN-only with no auth, this defense is "free" via Pydantic and worth keeping

### Read-only enforcement (AC 5, 6)
[Source: epics.md FR-2.21]

- The page renders ONLY the results display: label heading, ranked summary, `ResultsTable`
- It does NOT include:
  - "Finalise Session" button (live-only)
  - "Return to Scoring" button (live-only)
  - "Delete" button (history is append-only — never appears)
  - "Edit" button (none exists anywhere in v1)
- Tests assert these are absent in the DOM
- The `MainLayout` back arrow and home button are the ONLY navigation affordances

### `ResultsTable.vue` reuse contract
[Source: Story 2.5]

- `ResultsTable` props: `session: SessionData`
- Same component must work for live results and history detail [AC 1, 2]
- The component MUST NOT branch on `session.status` for layout — both states render identically
- If Story 2.5 introduced any action buttons inside `ResultsTable`, they must be moved OUT to the page level (so history detail can omit them)

### Visual specification
[Source: DESIGN.md, EXPERIENCE.md — same as Story 2.5 results screen, minus action buttons]

- Page padding: 16px
- Session label: `display` (Roboto 700, 24px), `ink-primary`
- Ranked summary cards: `surface-card`, `rounded/md`, 72px min height, rank chip + name + total
- ResultsTable: `surface-card`, `rounded/md`, sticky header, `score` typography for numerics
- All states: silent loading (centered `QSpinner` `accent`); error banner with terse copy

### Voice/tone strings
[Source: EXPERIENCE.md#Voice and Tone]

- Page title: "Session" (or display the session label itself)
- Loading: silent (spinner only)
- 404 error: "Session {label} not found." (one sentence, period)
- Other errors: "Couldn't load session."

### File locations (created or modified)
[Source: architecture.md#Complete Project Directory Structure]

**Frontend:**
- `frontend/src/apps/archery/pages/HistoryDetailPage.vue` *(new)*
- `frontend/src/apps/archery/stores/useArcheryHistoryStore.ts` *(modify — add `detail`, `detailLoading`, `detailError`, `loadDetail`)*
- `frontend/src/router/index.ts` *(modify — add /archery/history/:label)*

**Backend:**
- `backend/app/services/archery_service.py` *(modify — add `read_finalised`)*
- `backend/app/routers/archery.py` *(modify — add GET /sessions/{label} AFTER the more-specific /sessions/in-progress routes; ensure regex pattern on label)*

### Constraints / anti-patterns
- ❌ Do not duplicate `ResultsTable` for the history page — reuse the Story 2.5 component [AC 1]
- ❌ Do not show any action buttons (Finalise, Return, Delete, Edit) on this page [AC 5, 6, FR-2.21]
- ❌ Do not register `GET /sessions/{label}` BEFORE `GET /sessions/in-progress` — routing order matters; the regex on `label` also defends but explicit order is clearest
- ❌ Do not return in-progress sessions via `GET /sessions/{label}` — that's the responsibility of `/sessions/in-progress` (Story 3.2)
- ❌ Do not store `detail` and list `summaries` in the same ref — keep separate so navigating between list and detail does not lose state
- ❌ Do not envelope the response [Source: architecture.md#Anti-Patterns to Avoid]

### Testing

- **Backend tests:** pytest + FastAPI `TestClient` + `tmp_path` redirection of `DATA_DIR`. Cover: happy path, 404 missing, 422 malformed label (incl. traversal attempts), defensive 404 when file exists but is in-progress.
- **Frontend tests:** Vitest + `createTestingPinia` + `vue-router` mock. Verify:
  - Component mounts and calls `loadDetail` with the route's label
  - `ResultsTable` is rendered with the correct session prop
  - Action buttons are absent (specifically the strings "Finalise", "Return to Scoring", "Delete", "Edit")
  - 404 path: error state shown, no `ResultsTable`
- **Manual verification:**
  - Open History page; tap a session row → detail page renders identical layout to live results
  - Verify back arrow returns to history list with list state preserved
  - Verify NO buttons besides the navigation chrome
  - Try a manual URL with an invalid label (`/archery/history/foo`) → backend returns 422; FE shows error banner
  - Try a URL with a non-existent valid-format label (`/archery/history/1999-01-01`) → 404; FE shows error banner
  - Confirm shared component: a finalised session viewed in History Detail looks IDENTICAL to the same session viewed live (modulo the absence of action buttons)
- **Edge cases:**
  - Session with 1 archer: ranked summary still works (1 row); ResultsTable has one archer column
  - Very long archer names: ResultsTable horizontal-scrolls (sticky header preserved)
  - Browser back from detail to list → list state survives (Pinia store retains `summaries`)

## Change Log

| Date       | Version | Description                  | Author |
|------------|---------|------------------------------|--------|
| 2026-05-28 | 0.1     | Initial draft from epic 4.2  | Bob (SM) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

None — all tests passed green on first run.

### Completion Notes List

- `read_finalised()` in the service raises `FileNotFoundError` both when the file is absent AND when the file exists but has `status != "finalised"` — this defensively prevents the in-progress session from leaking through this endpoint.
- `GET /sessions/{label}` is registered last in `archery.py`, after all `/sessions/in-progress` routes and `/sessions` list route — FastAPI matches the more-specific literal paths first, so `in-progress` is never captured by the `{label}` wildcard.
- The `Path(..., pattern=_LABEL_PATTERN)` regex `^\d{4}-\d{2}-\d{2}(-\d+)?$` blocks path traversal (`../`), underscore-prefixed values (`_in_progress`), and arbitrary strings — FastAPI returns 422 automatically.
- `loadDetail()` in the store branches on `e?.status === 404` to give a user-friendly "Session X not found." message; other errors use the raw message.
- `HistoryDetailPage.vue` is strictly read-only: no Finalise, Return to Scoring, Delete, or Edit buttons anywhere in the template — tests assert their absence from the rendered text.
- `ResultsTable` and `ranked()` composable are reused unchanged from Story 2.5 — zero duplication.
- 8 backend integration tests; store spec extended with 4 `loadDetail` cases; 5 `HistoryDetailPage` component tests.

### File List

**Modified:**
- `backend/app/services/archery_service.py` — added `read_finalised()`
- `backend/app/routers/archery.py` — added `GET /sessions/{label}` with `Path` regex; `Path` import added (shared with router file)
- `frontend/src/apps/archery/stores/useArcheryHistoryStore.ts` — added `detail`, `detailLoading`, `detailError` refs; added `loadDetail()` action
- `frontend/src/apps/archery/stores/useArcheryHistoryStore.spec.ts` — added `loadDetail` describe block (4 tests)

**Created:**
- `backend/tests/test_archery_get_session.py` — 8 integration tests for `GET /api/archery/sessions/{label}`
- `frontend/src/apps/archery/pages/HistoryDetailPage.vue` — read-only detail page reusing `ResultsTable` and `ranked()`
- `frontend/src/apps/archery/pages/HistoryDetailPage.spec.ts` — 5 component tests

## QA Results

_To be populated by QA agent._
