---
baseline_commit: 9dd5dce6ca5870eaa5ec0f9ac4b1ff88f50fdebb
---

# Story 1.5: JWT Authentication

## Status

review

## Story

**As a** user of Application Dock,
**I want** to log in with a username and password and stay logged in for 7 days,
**so that** only authorised people can access the app and its data.

## Acceptance Criteria

1. **Given** no valid JWT token is present, **When** any `/api/*` route is called (except `/api/health` and `/api/auth/login`), **Then** the server returns `401 Unauthorized`.
2. **Given** valid credentials are submitted to `POST /api/auth/login`, **When** the server processes the request, **Then** it returns `{ access_token, token_type: "bearer" }` with a token valid for 7 days.
3. **Given** invalid credentials are submitted to `POST /api/auth/login`, **When** the server processes the request, **Then** it returns `401 Unauthorized` with `{ detail: "Invalid credentials" }`.
4. **Given** a valid JWT token, **When** `GET /api/auth/me` is called, **Then** it returns `{ username }` for the authenticated user.
5. **Given** credentials have been set up with the setup script, **When** the app starts, **Then** it reads credentials from `{data_dir}/_auth.json` and all protected routes are accessible with the resulting token.
6. **Given** the user is unauthenticated in the browser, **When** they navigate to any route, **Then** they are redirected to `/login`.
7. **Given** the user logs in successfully in the browser, **When** they navigate to `/login` again, **Then** they are redirected to `/`.
8. **Given** the user is logged in, **When** they refresh the browser, **Then** they remain logged in (token persisted in `localStorage`).
9. **Given** the user is logged in, **When** they click Logout, **Then** they are returned to `/login` and the token is cleared.
10. **Given** the token expires or is invalid, **When** the frontend makes any API call, **Then** the user is automatically redirected to `/login`.

## Tasks / Subtasks

- [x] **Task 1 — Backend: packages and config** (AC: 1–5)
  - [x] Add to `backend/requirements.txt`: `python-jose[cryptography]>=3.3`, `bcrypt>=4.0`
  - [x] Add to `backend/app/core/config.py` (`Settings`): `jwt_secret_key: str`, `jwt_algorithm: str = "HS256"`, `jwt_expire_days: int = 7`
  - [x] Document `JWT_SECRET_KEY` in `backend/.env.example`

- [x] **Task 2 — Backend: auth schemas** (AC: 2, 4)
  - [x] Create `backend/app/schemas/auth.py`:
    - `LoginRequest(BaseModel)`: `username: str`, `password: str`
    - `TokenResponse(BaseModel)`: `access_token: str`, `token_type: str = "bearer"`

- [x] **Task 3 — Backend: auth repository** (AC: 5)
  - [x] Create `backend/app/repositories/auth_repo.py`
  - [x] Define `UserRecord(BaseModel)`: `username: str`, `password_hash: str`
  - [x] `read_user() -> UserRecord | None` — reads `{data_dir}/_auth.json`; returns `None` if file absent
  - [x] `write_user(record: UserRecord) -> None` — writes via `_atomic_write_json` (same pattern as `session_repo.py`)

- [x] **Task 4 — Backend: auth service** (AC: 2, 3, 5)
  - [x] Create `backend/app/services/auth_service.py`
  - [x] `verify_login(username: str, password: str) -> bool` — reads user from `auth_repo`, verifies bcrypt hash; returns `False` if no user file exists
  - [x] `create_access_token(username: str) -> str` — creates signed JWT using `jose.jwt.encode` with `exp` claim set to `now + jwt_expire_days`
  - [x] `decode_token(token: str) -> str` — decodes and validates JWT; raises `ValueError` if invalid or expired; returns `username`

- [x] **Task 5 — Backend: dependency** (AC: 1)
  - [x] Create `backend/app/core/dependencies.py`
  - [x] `get_current_user(authorization: str | None = Header(None)) -> str` raises `HTTPException(401)` if missing or invalid token

- [x] **Task 6 — Backend: auth router** (AC: 2, 3, 4)
  - [x] Create `backend/app/routers/auth.py` with `prefix="/api/auth"`, `tags=["auth"]`
  - [x] `POST /login` → `TokenResponse` on success, `HTTPException(401)` on failure
  - [x] `GET /me` → `Depends(get_current_user)` → returns `{ "username": current_user }`
  - [x] Include `auth.router` in `backend/app/main.py`

- [x] **Task 7 — Backend: protect existing routes** (AC: 1)
  - [x] Add `_: str = Depends(get_current_user)` to every route function in `backend/app/routers/archery.py`
  - [x] Add `_: str = Depends(get_current_user)` to every route function in `backend/app/routers/shell.py`
  - [x] Leave `GET /api/health` unprotected

- [x] **Task 8 — Backend: setup script** (AC: 5)
  - [x] Create `backend/scripts/setup_auth.py`
  - [x] Script prompts for username and password interactively
  - [x] Hashes password with bcrypt and writes `_auth.json` via `auth_repo.write_user`
  - [x] Warns and asks to confirm if `_auth.json` already exists
  - [x] Document usage in `README.md`

- [x] **Task 9 — Backend: tests** (AC: 1–5)
  - [x] Create `backend/tests/test_auth.py`
  - [x] Fixture `auth_user` — writes a `_auth.json` with a known username/password via `auth_repo.write_user`
  - [x] `test_login_success` — valid credentials → 200 + token present
  - [x] `test_login_wrong_password` — 401
  - [x] `test_login_unknown_user` — no `_auth.json` exists → 401
  - [x] `test_protected_route_no_token` — `GET /api/archery/sessions` without token → 401
  - [x] `test_protected_route_valid_token` — `GET /api/archery/sessions` with valid token → 200
  - [x] `test_me_endpoint` — valid token → returns `{ "username": "..." }`

- [x] **Task 10 — Frontend: auth store** (AC: 6–10)
  - [x] Create `frontend/src/stores/useAuthStore.ts` (Pinia, composition API, follows `loading`/`error` pattern)
  - [x] State: `token`, `username`, `loading`, `error`
  - [x] `isAuthenticated = computed(() => token.value !== null)`
  - [x] `login(username, password)` — calls `POST /api/auth/login`, stores token in `localStorage`
  - [x] `logout()` — clears `localStorage` and resets store state

- [x] **Task 11 — Frontend: update useApi.ts** (AC: 10)
  - [x] Inject `Authorization: Bearer <token>` header when `authStore.token` is non-null
  - [x] On `ApiError` with `.status === 401` and a token was present: call `authStore.logout()` then redirect via `window.location.href`

- [x] **Task 12 — Frontend: login page** (AC: 6, 7)
  - [x] Create `frontend/src/pages/LoginPage.vue`
  - [x] Standalone minimal layout (no `MainLayout`)
  - [x] On submit: calls `authStore.login()`; on success redirects to `route.query.redirect ?? '/'`; on error displays `authStore.error`

- [x] **Task 13 — Frontend: router guard and route meta** (AC: 6, 7)
  - [x] Add `meta: { requiresAuth: true }` to all existing routes in `routes.ts`; add `/login` route
  - [x] Add `router.beforeEach` async guard in `index.ts`

- [x] **Task 14 — Frontend: logout button in MainLayout** (AC: 9)
  - [x] Add logout icon button in the header (only shown when `authStore.isAuthenticated`)
  - [x] On click: calls `authStore.logout()` then `router.push('/login')`

## Dev Notes

### New files
```
backend/app/schemas/auth.py
backend/app/repositories/auth_repo.py
backend/app/services/auth_service.py
backend/app/core/dependencies.py
backend/app/routers/auth.py
backend/scripts/setup_auth.py
backend/tests/test_auth.py
backend/tests/conftest.py
frontend/src/stores/useAuthStore.ts
frontend/src/pages/LoginPage.vue
frontend/src/router/typed-router.d.ts
```

### Modified files
```
backend/requirements.txt               — replaced passlib with bcrypt>=4.0
backend/app/core/config.py             — add JWT settings fields
backend/app/main.py                    — include auth router
backend/app/routers/archery.py         — add Depends(get_current_user) to all routes
backend/app/routers/shell.py           — add Depends(get_current_user) to all routes + fix typo
backend/.env.example                   — document JWT_SECRET_KEY
backend/README.md                      — document setup_auth.py usage
frontend/src/composables/useApi.ts     — inject auth header, handle 401 redirect
frontend/src/router/routes.ts          — add requiresAuth meta + /login route
frontend/src/router/index.ts           — add beforeEach guard
frontend/src/layouts/MainLayout.vue    — add logout button
frontend/src/layouts/MainLayout.spec.ts — add Pinia + mock authStore
frontend/src/composables/useApi.spec.ts — add Pinia + mock authStore
```

### Credential file format (`_auth.json`)
```json
{ "username": "admin", "password_hash": "$2b$12$..." }
```
Single-user file. Managed only via the setup script — no API endpoint to create/change users.

### JWT secret key
`JWT_SECRET_KEY` must be set as an env var (or in `.env`) in production. If left empty the service starts but tokens will be insecure (log a warning). Generate with: `python -c "import secrets; print(secrets.token_hex(32))"`.

### Token storage
Token stored in `localStorage` under key `auth_token`. Acceptable for self-hosted personal use. No refresh tokens — a single 7-day access token is issued.

### passlib → bcrypt direct
The story spec called for `passlib[bcrypt]` but `passlib` is incompatible with `bcrypt>=4.0`. Replaced with `bcrypt>=4.0` used directly. Same security guarantees, no compatibility shims.

### useApi circular dependency
Dynamic `import('@/stores/useAuthStore')` inside the `request()` function (not at module top-level) avoids the circular dependency at boot time.

### 401 redirect
`useApi.ts` only redirects to `/login` via `window.location.href` when `authStore.token !== null` at the time of the 401. This prevents redirect loops when a failed login attempt returns 401 (no token → just surface the error).

### Out of scope
- Multi-user support (adding/removing users via UI)
- Password change endpoint
- Refresh tokens
- MFA

## Change Log

| Date       | Version | Description                       | Author |
|------------|---------|-----------------------------------|--------|
| 2026-06-17 | 0.1     | Initial draft                     | Claude Code |
| 2026-06-17 | 0.2     | Implementation complete; all tests pass; status → review | Claude Code (dev agent) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes

- **passlib replaced with bcrypt direct**: `passlib[bcrypt]` is incompatible with `bcrypt>=5.0` (the installed version). Using `bcrypt` directly instead — same security, no dependency on the unmaintained passlib adapters.
- **conftest.py added**: Created `backend/tests/conftest.py` with `bypass_auth_for_non_auth_tests` autouse fixture using FastAPI `dependency_overrides`. This bypasses auth for all existing tests (which test archery/shell logic, not auth) without modifying 13 test files. `test_auth.py` is excluded from the bypass so it exercises the real auth flow.
- **useApi.ts 401 redirect**: Uses `window.location.href = '/login'` (not `router.push`) because `request()` is a plain async function, not a Vue composition context — `useRouter()` is unavailable there.
- **Pre-existing typo fixed**: "Archery Score Counterrr" → "Archery Score Counter" in `shell.py` (was failing a pre-existing test).
- **Pre-existing lint issue fixed**: `test_update_service.py` import ordering (ruff I001, auto-fixed).
- **ESLint broken pre-existing**: `npm run lint` fails due to missing `eslint.config.js` — pre-existing, not introduced by this story.
- **All backend tests**: 194/194 passing.
- **All frontend tests**: 179/179 passing across 21 test files.

### File List

**New files:**
- `backend/app/schemas/auth.py`
- `backend/app/repositories/auth_repo.py`
- `backend/app/services/auth_service.py`
- `backend/app/core/dependencies.py`
- `backend/app/routers/auth.py`
- `backend/scripts/setup_auth.py`
- `backend/tests/test_auth.py`
- `backend/tests/conftest.py`
- `frontend/src/stores/useAuthStore.ts`
- `frontend/src/pages/LoginPage.vue`
- `frontend/src/router/typed-router.d.ts`

**Modified files:**
- `backend/requirements.txt`
- `backend/app/core/config.py`
- `backend/app/main.py`
- `backend/app/routers/archery.py`
- `backend/app/routers/shell.py`
- `backend/.env.example`
- `README.md`
- `frontend/src/composables/useApi.ts`
- `frontend/src/router/routes.ts`
- `frontend/src/router/index.ts`
- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/layouts/MainLayout.spec.ts`
- `frontend/src/composables/useApi.spec.ts`
- `docs/stories/application-dock-general/1.5.jwt-authentication.story.md`

## QA Results

_To be populated by QA agent._
