---
baseline_commit: a867ce5b98eeb9f5f338749f8133117f3d42a9c9
depends_on: [1.3]
---

# Story 2.1: Add a todo as a colored pill

Status: Done

## Story

As a user,
I want a plus button that adds a todo with a header, text, and a color,
so that it appears as a big colored pill block on my board.

## Acceptance Criteria

> **Superseded by Story 2.8:** the `body` field was removed. Wherever an AC below says "header and body" / `{ header, body, color }`, the todo's content now lives in the append-only updates log — there is no `body`. The code is coherent with 2.8; these AC texts are retained for history.

1. **Board loads a list's active todos.** Given a list board (`/context-switch/lists/:listId`), when it loads, then it fetches the list with its active todos (`GET /api/context-switch/lists/{list_id}`) and renders each active todo as a large **pill block** showing its header and body text. (FR-6)

2. **Plus button adds a todo.** Given the board, when I click the **plus** button and enter a header and body text, then `POST /api/context-switch/lists/{list_id}/todos` with `{ header, body, color }` creates the todo (`status: "active"`, `order` = end of the active list) and it appears as a new pill. (FR-6)

3. **Color choice — preset or custom.** Given the add form, when I choose a color, then I can pick from a **preset palette** (fixed swatches) or set a **custom** color; the created pill renders in that color. (FR-7)

4. **Readable contrast.** Given a chosen pill color, when the pill renders, then its header/body text uses a foreground that stays readable against that background (e.g. compute light/dark text from the color's luminance). (FR-7, NFR-1)

5. **Header required.** Given a submission with an empty/whitespace header, when submitted, then it is rejected — the client disables submit and the API backstops with a `422` `{detail}`. Body may be empty.

6. **Persisted & isolated.** Given a created todo, when the board reloads (or another device logs in as me), then the todo is still there; another user never sees it. (FR-2)

## Tasks / Subtasks

- [x] **Task 1: Backend — get list + create todo** (AC: 1, 2, 5)
  - [x] `services/context_switch_service.py`: `get_list(username, list_id) -> TodoList` (active todos, ordered by `order`; raise `FileNotFoundError` if absent) and `add_todo(username, list_id, header, body, color) -> Todo` (validate header; mint `t-{uuid8}`; `status="active"`, `order`=max+1; timestamps; empty `updates`).
  - [x] `routers/context_switch.py`: `GET /lists/{list_id}` and `POST /lists/{list_id}/todos`; `FileNotFoundError` → 404, `ValueError` → 422.
  - [x] `schemas/context_switch.py`: `CreateTodoRequest(header, body, color)`; validate `color` is a hex string (`#rrggbb`).
- [x] **Task 2: Frontend — color picker + add dialog** (AC: 3, 4)
  - [x] `components/ColorPicker.vue` — preset swatches + a "custom" option (Quasar `q-color` or native input) → emits a hex.
  - [x] `components/AddTodoDialog.vue` — header input, body textarea, `ColorPicker`; submit disabled on blank header; emits the new-todo payload.
  - [x] Define the preset palette as a small constant (on-theme swatches).
- [x] **Task 3: Frontend — pill + board wiring** (AC: 1, 2, 4)
  - [x] `components/TodoPill.vue` — a large colored block rendering header + body; computes readable text color from the background hex.
  - [x] Extend the store: `fetchList(listId)`, `activeTodos`, `addTodo(listId, payload)`; `BoardPage.vue` renders pills + a floating/`+` add button opening `AddTodoDialog`.
- [x] **Task 4: Tests** (AC: 1–6)
  - [x] Backend: create returns a todo with an id + `status:"active"`; blank header → 422; created todo appears in `GET /lists/{id}`; isolation across users.
  - [x] Frontend: `AddTodoDialog.spec.ts` (submit disabled on blank header, emits payload), `TodoPill.spec.ts` (renders header/body, contrast helper picks readable text), board renders a pill after add.

## Dev Agent Record

### Agent Model Used

claude-fable-5

### Completion Notes

- **Backend:** `service.get_list` returns the list via `model_copy` carrying only its **active** todos sorted by `order` (archived todos get their own view in Story 2.7). `service.add_todo` trims and rejects a blank header (`ValueError`), mints `t-{uuid8}`, and sets `order = max(active order) + 1` so new todos append. Router adds `GET /lists/{list_id}` and `POST /lists/{list_id}/todos`; `FileNotFoundError` → 404, `ValueError` → 422.
- **Color validation** is a pydantic `pattern` on `CreateTodoRequest.color` (`HEX_COLOR_PATTERN`, `#rrggbb`), so a bad hex is a 422 straight from FastAPI — no service-layer branch needed.
- **Contrast helper is a pure module,** `apps/context-switch/colors.ts`: WCAG relative luminance + `readableTextColor` (black above 0.179 luminance, white below). Unit-tested directly, including a check that **every** preset swatch clears 4.5:1 against the chosen text color.
- **Components:** `ColorPicker` (12 preset swatches + a native `<input type="color">` for custom, both emitting the same `#rrggbb` via `v-model`), `AddTodoDialog` (header/body/color; submit disabled on a blank header; form resets on reopen), `TodoPill` (dumb presentational block — `defineProps<{ todo: Todo }>()`, background = `todo.color`, text = `readableTextColor`).
- **Store:** `currentList`, `activeTodos` (computed — filters archived, sorts by `order`), `fetchList` (nulls the board and surfaces the error on failure), `addTodo` (pushes the created todo locally, rethrows so the page can react). `BoardPage` fetches on mount and renders pills in an auto-fill grid; the fixed C×R grid arrives in Story 2.2.
- **Tests:** backend +12 (get list, unknown → 404, create shape, append order, trim, blank → 422, empty body, bad color → 422, unknown list → 404, cross-user isolation, active_count). Frontend +21 across `colors`, `ColorPicker`, `TodoPill`, `AddTodoDialog`, `BoardPage`, store. Full suites: backend **386**, frontend **406** — no regressions. `black`/`ruff`/Prettier clean.
- **Pre-existing repo issue (not touched):** `npm run lint` fails because the repo has no ESLint v9 `eslint.config.js` (only the old flat-config-less setup); this predates this story.

### File List

- `backend/app/schemas/context_switch.py` (modified) — `HEX_COLOR_PATTERN`, `CreateTodoRequest`
- `backend/app/services/context_switch_service.py` (modified) — `_active_sorted`, `get_list`, `add_todo`
- `backend/app/routers/context_switch.py` (modified) — `GET /lists/{id}`, `POST /lists/{id}/todos`
- `backend/tests/test_context_switch_api.py` (modified) — Story 2.1 cases
- `frontend/src/apps/context-switch/colors.ts` (new) + `colors.spec.ts` (new)
- `frontend/src/apps/context-switch/components/ColorPicker.vue` (new) + spec
- `frontend/src/apps/context-switch/components/AddTodoDialog.vue` (new) + spec
- `frontend/src/apps/context-switch/components/TodoPill.vue` (new) + spec
- `frontend/src/apps/context-switch/pages/BoardPage.vue` (modified) + spec (rewritten)
- `frontend/src/apps/context-switch/stores/useContextSwitchStore.ts` (modified) + spec
- `frontend/src/apps/context-switch/types.ts` (modified) — `NewTodo`

## Dev Notes

**Pill = the core visual.** The "big pill block" is the app's signature UI. Keep `TodoPill` a dumb presentational component (`defineProps<{ todo: Todo }>()`); the contrast/text-color helper is a small pure function (unit-test it directly).

**Color model.** Store the color as a hex string on the todo. Preset swatches and the custom picker both resolve to the same `#rrggbb` field — no separate "preset vs custom" flag needed (YAGNI). Validate hex server-side.

**Ordering.** New todos append at the end of the active order; a numeric `order` field on each todo is the source of truth (drag reorder in Story 2.3 rewrites it). Archived todos are excluded from the active-order sequence.

**Coding standards.** `<script setup lang="ts">`, `defineProps<{}>()`/`defineEmits<{}>()`, `interface` for object shapes, no `any` without justification; all HTTP via `useApi`; store exposes `loading`/`error`. [Source: CLAUDE.md#Conventions/Frontend]

### Project Structure Notes

- NEW: `frontend/src/apps/context-switch/components/{ColorPicker,AddTodoDialog,TodoPill}.vue` + specs.
- UPDATE: `BoardPage.vue`, `useContextSwitchStore.ts`, `routers/context_switch.py`, `services/context_switch_service.py`, `schemas/context_switch.py`, backend api tests.

### References

- [Source: docs/planning-artifacts/epics-context-switch.md#Story 2.1 / FR-6, FR-7]
- Pattern refs: existing archery/hotaru dialog + component patterns under `frontend/src/apps/*/components/`.

## Review Findings

_Code review (2026-08-15) — bmad-code-review, 3 adversarial layers x 2 chunks (backend + frontend): Blind Hunter, Edge Case Hunter, Acceptance Auditor._

- [x] [Review][Patch] BoardPage renders the "No todos yet" empty-state AND the error banner together on a load failure. **Fixed:** the empty-state/board are now gated on `store.currentList` (a failed load leaves it null → only the error banner shows), while the error banner stays independent so a *mutation* error keeps the board visible. Added regression assertions to `BoardPage.spec.ts` ("surfaces a load failure"). [frontend/src/apps/context-switch/pages/BoardPage.vue]
- [x] [Review][Defer] AddTodoDialog onSubmit emits create then calls close() synchronously — the parent awaits a non-optimistic addTodo, so on POST failure the dialog is gone and the typed header/first-update/color are lost. Recommend keeping the dialog open until the persist resolves. [components/AddTodoDialog.vue] — deferred, UX / behavior change needs test updates
- [x] [Review][Defer] AddTodoDialog has no double-submit guard — a rapid second click on Add can emit two creates -> duplicate todos. [components/AddTodoDialog.vue] — deferred, low likelihood
- [x] [Review][Defer] store.addTodo optional-chains currentList.value?.todos.push — if currentList is null the POST succeeds but the todo silently vanishes from the UI. [stores/useContextSwitchStore.ts] — deferred, edge
- [x] [Review][Defer] No maximum length on a todo header. [backend/app/schemas/context_switch.py] — deferred, robustness / no requirement
