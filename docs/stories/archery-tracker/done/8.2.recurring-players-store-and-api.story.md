# Story 8.2: Backend — Recurring Players Store & API

## Status

Ready for Review

## Story

**As a** developer,
**I want** a server-side recurring-players list behind an API,
**so that** operators can maintain a reusable roster across devices on the LAN.

## Acceptance Criteria

1. **Given** persistence, **When** recurring players are saved, **Then** they are stored in `DATA_DIR/_recurring_players.json` (underscore-prefixed, excluded from history) via the atomic write pattern in `session_repo.py`.
2. **Given** the repository layer, **When** `read_recurring_players()` / `write_recurring_players(names)` are called, **Then** they handle file-absent (returns empty list) and write atomically; no `HTTPException` raised in the repo.
3. **Given** `GET /api/archery/players`, **When** called, **Then** it returns a direct JSON array of recurring player names.
4. **Given** `POST /api/archery/players` with a name, **When** called, **Then** the name is added (trimmed, non-empty, de-duplicated case-sensitively per existing roster rules) and the updated list returned; adding a duplicate is a no-op success.
5. **Given** `DELETE /api/archery/players/{name}`, **When** called, **Then** the name is removed if present; idempotent.
6. **Given** the service layer, **When** it mediates these calls, **Then** validation lives in `archery_service.py` and file I/O only in `session_repo.py` (layering preserved).

## Tasks / Subtasks

- [x] **Task 1 — Repository** (AC: 1, 2)
  - [x] In `session_repo.py`, add `_RECURRING_PLAYERS_FILENAME = "_recurring_players.json"`.
  - [x] `read_recurring_players() -> list[str]`: return `[]` if the file is absent; else parse the JSON array. Skip/repair malformed gracefully (return `[]` + warn).
  - [x] `write_recurring_players(names: list[str]) -> None`: write via `_atomic_write_json` (the helper currently types payload as `dict` — either generalise it to accept a list, or add a small list-aware atomic writer; keep the write-`.tmp`-then-`os.replace` pattern).
  - [x] Confirm `_recurring_players.json` is excluded from `list_sessions()` (already excludes `_`-prefixed files).

- [x] **Task 2 — Schema** (AC: 4)
  - [x] In `schemas/session.py` (or a new `schemas/players.py`), add `AddPlayerRequest { name: str }` with the same trim/non-empty validation used for archer names.

- [x] **Task 3 — Service** (AC: 4, 5, 6)
  - [x] In `archery_service.py`, add:
    - `list_recurring_players() -> list[str]`
    - `add_recurring_player(name) -> list[str]`: trim; reject empty (raise `ValueError`); if already present, return unchanged (no-op); else append and persist; return the list.
    - `remove_recurring_player(name) -> list[str]`: remove if present (idempotent); persist; return the list.
  - [x] All file access through `session_repo` only.

- [x] **Task 4 — Routers** (AC: 3, 4, 5)
  - [x] In `routers/archery.py`:
    - `GET /api/archery/players` → `list[str]`.
    - `POST /api/archery/players` (body `AddPlayerRequest`) → `list[str]`; translate `ValueError` → 422/400.
    - `DELETE /api/archery/players/{name}` → 200 with updated list (or 204). Path param `name` URL-decoded by FastAPI.
  - [x] Keep endpoints under the existing `/api/archery` router prefix; kebab-case plural noun `players`.

- [x] **Task 5 — Tests** (AC: 1–6)
  - [x] Repo: read-absent → `[]`; round-trip write/read; atomicity (tmp then replace).
  - [x] Service: add new; add duplicate no-op; add empty → error; remove present/absent.
  - [x] Router: GET array; POST returns updated list; DELETE idempotent.

## Dev Notes

### Previous Story Insights
- Independent of the multi-session work, but lives in the same modules. Can land in parallel with Epic 6/7, ideally before Stories 8.3/8.4 (the consuming UI).
- `session_repo._atomic_write_json(path, payload: dict)` currently assumes a dict. Recurring players are a JSON array — either widen the signature to `dict | list` or add `_atomic_write_text`/a list variant. Keep the `.tmp` → `os.replace()` guarantee.
- Archer-name validation rules to mirror (from `CreateSessionRequest`/`SessionData`): trim whitespace, reject empty, de-dup. Recurring-player uniqueness is within the players list.

### Source references
- `backend/app/repositories/session_repo.py` — `_atomic_write_json`, `list_sessions` (underscore exclusion), file path conventions.
- `backend/app/services/archery_service.py` — layering reference; add player functions here.
- `backend/app/routers/archery.py` — `APIRouter(prefix="/api/archery")`, existing endpoint style, `HTTPException` translation.
- `backend/app/schemas/session.py` — archer-name validators to mirror.
- Architecture: `docs/architecture/source-tree.md` (layering, file I/O only in repo; JSON fields snake_case; endpoints kebab-case plural).

### Storage shape
`_recurring_players.json` = a flat JSON array of strings, e.g. `["Alice", "Bob", "Cara"]`. No schema versioning needed in v1.

### Constraints / anti-patterns
- ❌ No file I/O outside `session_repo.py`.
- ❌ No `HTTPException` in repo or service — raise stdlib (`ValueError`), translate in the router.
- ❌ Don't put this list in a database — JSON file only (tech-stack v1 constraint).

### Testing
- **Backend:** pytest + `TestClient`. Temp `DATA_DIR`. Cover repo, service, and router layers.

## Change Log

| Date       | Version | Description                                    | Author      |
|------------|---------|------------------------------------------------|-------------|
| 2026-05-29 | 0.1     | Initial draft                                  | Sarah (PO)  |
| 2026-05-29 | 1.0     | Implemented recurring-players store + API; tests pass | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context)

### Debug Log References

None. Backend `pytest` 158 passed (17 new); `ruff`/`black` clean.

### Completion Notes List

- **Repository:** `_recurring_players.json` (underscore-prefixed → excluded from `list_sessions()`). `read_recurring_players()` returns `[]` when absent or corrupt (warns); `write_recurring_players()` reuses the atomic write. Widened `_atomic_write_json`'s type hint to `dict[str, object] | list[str]` (the helper already `json.dumps`-es either shape).
- **Schema:** `AddPlayerRequest { name: str }` with trim + non-empty validation (mirrors archer-name rules).
- **Service:** `list_recurring_players`, `add_recurring_player` (trim, reject empty via `ValueError`, de-dup no-op), `remove_recurring_player` (idempotent). File I/O stays in the repo; validation in the service.
- **Router:** `GET /api/archery/players` → `list[str]`; `POST /api/archery/players` → updated list (translates `ValueError` → 422); `DELETE /api/archery/players/{name}` → updated list, idempotent. No path collision with `/sessions`.
- Storage shape is a flat JSON array of strings.

### File List

**Backend — modified:**
- `backend/app/repositories/session_repo.py`
- `backend/app/schemas/session.py` (`AddPlayerRequest`)
- `backend/app/services/archery_service.py`
- `backend/app/routers/archery.py`

**Backend — created:**
- `backend/tests/test_archery_players.py`

## QA Results

_To be populated by QA agent._

## Review Findings

### Tasks / Subtasks — Review Items

- [x] [Review][Patch] `AddPlayerRequest` (and thus `POST /api/archery/players`) does not reject names containing `/` — a player added with a `/` in their name can never be removed via `DELETE /players/{name}` because the path segment splits [`backend/app/schemas/session.py:175–185`; fix: add `_no_slash` validator rejecting names containing `/`]
