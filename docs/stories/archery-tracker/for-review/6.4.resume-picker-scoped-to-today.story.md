# Story 6.4: Resume Picker Scoped to Today

## Status

Ready for Review

## Story

**As an** operator,
**I want** to pick which of today's in-progress sessions to resume,
**so that** I can continue the right round when several are open.

## Acceptance Criteria

1. **Given** the Archery home screen, **When** no in-progress session is dated the current UTC day, **Then** the **Resume** button is hidden (FR-6.2).
2. **Given** one or more in-progress sessions are dated the current UTC day, **When** the home screen renders, **Then** the **Resume** button is shown.
3. **Given** the operator taps **Resume**, **When** there is exactly one of today's open sessions, **Then** it resumes directly to the scoring board in exact restored state.
4. **Given** the operator taps **Resume** and there are multiple of today's open sessions, **When** the tap registers, **Then** a picker lists them (name + confirmed-target count) and selecting one resumes it to exact state.
5. **Given** the picker, **When** it lists sessions, **Then** only the current UTC day's in-progress sessions are shown (older open sessions are excluded from Resume but still counted toward the New Session conflict popup).
6. **Given** a resumed session, **When** the scoring board renders, **Then** confirmed targets are green and unconfirmed targets are open — state is server-side and resumable from any LAN device.

## Tasks / Subtasks

- [x] **Task 1 — "Today" filter** (AC: 1, 2, 5)
  - [x] Compute today's UTC date string once: `new Date().toISOString().slice(0, 10)`.
  - [x] Derive `todaysInProgress = inProgressList.filter(s => s.date === today)` from the list loaded in Story 6.3.
  - [x] Resume button `v-if="todaysInProgress.length > 0"`.

- [x] **Task 2 — Resume action: direct vs picker** (AC: 3, 4)
  - [x] If `todaysInProgress.length === 1` → resume that session's label directly.
  - [x] If `> 1` → open a `q-dialog`/list picker showing each session's `name` (via `displaySessionName`) and `${targets.length} of 18 confirmed`; selecting a row resumes that label.

- [x] **Task 3 — Resume by label** (AC: 3, 4, 6)
  - [x] Add `resumeSession(label)` to `useArcherySessionStore`: `GET /api/archery/sessions/in-progress/{label}` → set `session.value`, then component routes to `/archery/scoring`.
  - [x] Ensure `ScoringBoardPage` renders green/open state from the restored `session.targets` + per-target `confirmed` flag (Story 7.1). If 7.1 not yet merged, fall back to "target present == confirmed".

- [x] **Task 4 — Picker component** (AC: 4)
  - [x] Inline the picker in `ArcheryHomePage.vue` (simple `q-list` of buttons) or a small `ResumePicker.vue` under `apps/archery/components/`. Each row ≥44px, shows name + confirmed count.

- [x] **Task 5 — Tests** (AC: 1–6)
  - [x] `ArcheryHomePage.spec.ts`: Resume hidden when no today sessions; shown when ≥1; single → direct resume+route; multiple → picker, selection resumes the chosen label.
  - [x] `useArcherySessionStore.spec.ts`: `resumeSession(label)` fetches the right URL and sets `session`.

## Dev Notes

### Previous Story Insights
- **Depends on Story 6.1** (per-label `GET /api/archery/sessions/in-progress/{label}`, array list with `date`) and **Story 6.3** (loads `inProgressList`, lays out buttons). **Depends on Story 6.2** for `name`/`displaySessionName`. **Coordinates with Story 7.1** for the per-target `confirmed` flag used to colour the board.
- The old single-session `resumeSession()` (no args) in `useArcherySessionStore` resumed `_in_progress.json`. This story replaces it with a label-scoped version.

### Source references
- `frontend/src/apps/archery/pages/ArcheryHomePage.vue` — Resume button + picker.
- `frontend/src/apps/archery/stores/useArcherySessionStore.ts` — add `resumeSession(label)`.
- `frontend/src/apps/archery/pages/ScoringBoardPage.vue` — renders restored state; `isConfirmed(n)` currently = "target exists"; with Story 7.1 it becomes the target's `confirmed` flag.
- `frontend/src/apps/archery/composables/useSessionLabel.ts` — `displaySessionName` (Story 6.2).

### Why "today" only for Resume
FR-6.2: Resume is for continuing *today's* rounds. Older open sessions still exist (and are counted by the New Session "delete or leave" popup in Story 6.3) but are intentionally not offered for resume on the home screen, to keep the picker focused. Date comparison uses the session `date` field (UTC `YYYY-MM-DD`) from Story 6.1.

### Constraints / anti-patterns
- ❌ Don't filter by `label` prefix for "today" — use the explicit `date` field (a custom-named session still has today's `date`).
- ❌ All HTTP via `useApi.ts`.
- ❌ Don't auto-resume on load — Resume is always an explicit tap.

### Testing
- **Frontend:** Vitest + `@vue/test-utils` + `@pinia/testing`. Provide a mocked store with controllable `inProgressList` (mix of today and older dates) and assert button visibility, picker contents, and resume routing.

## Change Log

| Date       | Version | Description                                      | Author      |
|------------|---------|--------------------------------------------------|-------------|
| 2026-05-29 | 0.1     | Initial draft                                    | Sarah (PO)  |
| 2026-05-29 | 1.0     | Implemented today-scoped resume picker; tests pass | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context)

### Debug Log References

None. Frontend green: `vitest` 119 passed, `vue-tsc --noEmit` clean.

### Completion Notes List

- `todaysInProgress` computed filters `store.inProgressList` by the explicit `date` field against the current UTC day (`new Date().toISOString().slice(0, 10)`), per FR-6.2 — not by label prefix, so a custom-named session still matches today.
- **Resume** button is `v-if="todaysInProgress.length > 0"` — hidden when none of today's sessions are open (older open sessions still count toward the New Session conflict popup but are excluded here).
- Tapping Resume with exactly one of today's sessions resumes it directly; with more than one it opens a picker (`q-list` of rows showing `displaySessionName` + "{n} of 18 confirmed"). Selecting a row calls `store.resumeSession(label)` and routes to `/archery/scoring`.
- Added the label-scoped `resumeSession(label)` store action (replaces the 6.1 shim's no-arg version), which `GET`s `/archery/sessions/in-progress/{label}` and sets `store.session`.
- Board state colouring still uses target presence (`isConfirmed`); the per-target `confirmed` flag introduced in Story 7.1 is wired into the board in Stories 7.2/7.3.

### File List

**Frontend — modified:**
- `frontend/src/apps/archery/pages/ArcheryHomePage.vue` (Resume button, picker)
- `frontend/src/apps/archery/pages/ArcheryHomePage.spec.ts`
- `frontend/src/apps/archery/stores/useArcherySessionStore.ts` (`resumeSession(label)`)
- `frontend/src/apps/archery/stores/useArcherySessionStore.spec.ts`

## QA Results

_To be populated by QA agent._
