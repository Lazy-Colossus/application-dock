---
baseline_commit: 1bbe2b4ef62eaea2ffc5ed85ef28f337a010d71f
---

# Story 8.4: Pick Recurring Players in Session Setup

## Status

review

## Story

**As an** operator,
**I want** to add recurring players to a new session's roster from a picker,
**so that** I can build a roster quickly without retyping familiar names.

## Acceptance Criteria

1. **Given** the session setup screen, **When** it renders, **Then** the free-text custom-name input remains, plus a control to pick from recurring players.
2. **Given** the operator opens the recurring-players picker, **When** it loads, **Then** it lists recurring players not already in the draft roster.
3. **Given** the operator selects a recurring player, **When** the selection registers, **Then** that name is added to the draft roster (same uniqueness rules as typed names).
4. **Given** a recurring player already in the roster, **When** the picker renders, **Then** it is hidden or disabled to prevent duplicates.
5. **Given** no recurring players exist, **When** the picker is opened, **Then** an empty state points the operator to the management view (Story 8.3).

## Tasks / Subtasks

- [x] **Task 1 — Load recurring players on setup** (AC: 1, 2)
  - [x] In `SessionSetupPage.vue`, use `useRecurringPlayersStore` (Story 8.3) and call `loadPlayers()` on mount.
  - [x] Add a "From list" button opening an inline picker next to the custom-name input.

- [x] **Task 2 — Picker contents & dedup** (AC: 2, 4)
  - [x] Compute `availablePlayers = recurringPlayers.filter(n => !store.draftRoster.includes(n))`.
  - [x] Render only `availablePlayers` (already-rostered names excluded).

- [x] **Task 3 — Add on select** (AC: 3)
  - [x] On selecting a recurring name, push it to `store.draftRoster` via extracted `addName(name)` helper — same trim/non-empty/duplicate guard as `addArcher()`. Picker closes on selection.

- [x] **Task 4 — Empty state** (AC: 5)
  - [x] If `recurringStore.players.length === 0`, the picker shows "No recurring players — add some in Manage Players" with a link to `/archery/players`.

- [x] **Task 5 — Tests** (AC: 1–5)
  - [x] `SessionSetupPage.spec.ts`: `loadPlayers` called on mount; picker button visible; picker hidden by default; open reveals picker; lists only non-rostered names; rostered names excluded; selecting adds to roster and closes picker; empty state links to management.

## Dev Notes

### Previous Story Insights
- **Depends on Stories 8.2 (API) and 8.3 (`useRecurringPlayersStore`).** Reuses `store.draftRoster` and the `addArcher` validation from Story 2.2.
- This page also gains a "Session name" input (Story 6.2) and focus retention (Story 8.1). Keep the layout coherent: Session name → custom archer input + Add + "From list" → picker dropdown → roster list → Confirm.
- Extract the name-add validation into a shared helper so the custom input, Enter key, and recurring picker all enforce identical rules (avoids divergence).

### Source references
- `frontend/src/apps/archery/pages/SessionSetupPage.vue` — `addArcher()`, `store.draftRoster`, roster list rendering.
- `frontend/src/apps/archery/stores/useRecurringPlayersStore.ts` — `players`, `loadPlayers()` (Story 8.3).
- `frontend/src/apps/archery/stores/useArcherySessionStore.ts` — `draftRoster`, `draftName`.
- `frontend/src/router/routes.ts` — `/archery/players` route (Story 8.3) for the empty-state link.

### UX note
The custom free-text field MUST remain (FR-8.3) — the picker augments, not replaces, it. A one-off guest archer can still be typed without becoming a recurring player.

### Constraints / anti-patterns
- ❌ Don't auto-add all recurring players — selection is explicit.
- ❌ Don't bypass roster uniqueness/trim rules for picked names.
- ❌ All HTTP via the store/`useApi.ts`.

### Testing
- **Frontend:** Vitest + `@vue/test-utils` + `@pinia/testing`. Provide a stubbed `useRecurringPlayersStore` with controllable `players`. Assert filtering against `draftRoster`, add-on-select, and empty-state behaviour.

## Change Log

| Date       | Version | Description                        | Author       |
|------------|---------|------------------------------------|--------------|
| 2026-05-29 | 0.1     | Initial draft                      | Sarah (PO)   |
| 2026-06-10 | 1.0     | Implemented — all tasks complete   | Amelia (Dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

None — clean run.

### Completion Notes List

- Task 1: `useRecurringPlayersStore` imported into `SessionSetupPage.vue`; `loadPlayers()` called in `onMounted` alongside date preload. "From list" `q-btn` added in the input row.
- Task 2: `availablePlayers` computed ref filters `recurringStore.players` against `store.draftRoster`; only available names shown in picker.
- Task 3: `addName(name)` helper extracted from `addArcher()` — shared validation (non-empty, no duplicate, push to roster). `addArcher` calls `addName` + clears input + refocuses. `pickPlayer(name)` calls `addName` + closes picker.
- Task 4: Picker shows empty-state `<p>` with `router-link` to `/archery/players` when `recurringStore.players.length === 0`.
- Task 5: 10 new tests added to `SessionSetupPage.spec.ts` covering all ACs; existing 9 tests migrated to `data-testid="add-btn"` selector; 19 total tests green.
- Note: picker uses an inline `v-if` div with native `<button>` items styled via scoped SASS; `text-transform: capitalize` applied so lowercase stored names display properly.

### File List

- `frontend/src/apps/archery/pages/SessionSetupPage.vue` — modified (picker, `addName` helper, `useRecurringPlayersStore`)
- `frontend/src/apps/archery/pages/SessionSetupPage.spec.ts` — modified (10 new picker tests, `add-btn` data-testid, `/archery/players` route, `del` mock)

## QA Results

_To be populated by QA agent._
