---
baseline_commit: 6a51dc978b3006630cba944179f7939e4a1c7905
depends_on: [1.1, "application-dock-general/1.8"]
---

# Story 1.3: Shared per-day data model, repository & per-day write serialization

## Status

Ready for Review

## Story

**As** the platform,
**I want** each day stored as one JSON file keyed by date, with every read-modify-write on a day
serialized against concurrent writers,
**so that** many people answering the same question at once never erase each other's answers.

## Acceptance Criteria

1. **Schemas.** Given `backend/app/schemas/qotd.py`, when models are defined, then Pydantic v2
   models exist for `Answer` (`text: str | None`, `rating: int | None`, `created_at`, `updated_at`)
   and `Day` (`schema_version`, `date`, `question_id`, `answers: dict[str, Answer]`), with a
   model-level rule that an answer sets **exactly one** of `text` / `rating`. (AR-5)

2. **One file per month, no index.** Given `backend/app/repositories/qotd_repo.py`, when it reads and
   writes, then it is the only module touching the filesystem, stores each month's days in one
   `Month` document at `DATA_DIR/qotd/months/{YYYY-MM}.json`, `mkdir(parents=True, exist_ok=True)` on
   write, routes every write through `core.storage.atomic_write_json`, returns a fresh empty `Day` for
   a date with no entry yet, and lists days by **scanning the months directory** and flattening — no
   index file to become a second contention point. Callers still work a **day** at a time; only the
   on-disk layout is monthly (chosen so the folder stays small and human-legible — ~12 files a year
   rather than ~365). (NFR-2, NFR-3, AR-2)

3. **Shared, not per-user.** Given any repo function, when it resolves a path, then the path is
   derived from the **date only**. No function takes a username for file selection; the username
   only decides *whose* answer *within* a day. (FR-2, NFR-2)

4. **Date validation blocks traversal.** Given a date value that is not a canonical `YYYY-MM-DD`
   (separators, `..`, padding, wrong shape), when the repo derives a path from it, then it raises
   `ValueError` rather than reading or writing outside the store. (NFR-2)

5. **Transactions consume the platform lock.** Given platform Story 1.8 has landed, when the repo
   serializes a mutation, then it uses `core.locks.key_lock` via a `day_transaction(date)` context
   manager that holds the lock across read→write and writes nothing if the block raises — it does
   **not** define a QotD-local lock. (NFR-1)

6. **Concurrent answers all survive.** Given N threads upserting answers on the **same** day, when
   they interleave on the threadpool, then every answer is applied and none is lost; and given
   mutations of **different months**, then they are not serialized against each other (days in the
   *same* month share the month-file lock and so serialize — cheap and correct at this scale). Both
   are asserted by tests verified to fail with the lock disabled. (NFR-1)

7. **Server-side clock helper.** Given `backend/app/services/qotd_service.py`, when it decides
   "today" or past-vs-present, then it does so through a single helper (`today()` / `now_iso()`)
   that every later story shares, and `today` itself counts as still-writable. (NFR-5)

8. **Layering holds.** Given the service, when it operates on a document, then it raises stdlib
   exceptions only — `FileNotFoundError`, `ValueError` — and never `HTTPException`. (NFR-4)

9. **No regressions.** Given the existing suites, when this story lands, then `pytest` stays green
   and `black` / `ruff` are clean. Frontend untouched.

## Tasks / Subtasks

- [x] **Task 1: Schemas** (AC: 1)
  - [x] Add `Answer` and `Day` to `backend/app/schemas/qotd.py`; enforce exactly-one-of
        `text`/`rating` with a model validator. `answers` keyed by username.
- [x] **Task 2: Repository** (AC: 2, 3, 4, 5)
  - [x] Create `backend/app/repositories/qotd_repo.py`: `_month_path(date)` with `_validate_date`,
        `read_day`, `write_day`, `list_days` (months-directory scan, flattened), `migrate`, and
        `day_transaction(date)`. Days are bucketed into one `Month` file per month.
  - [x] `day_transaction` wraps `key_lock(str(month_path))`, reads the month, yields the day, writes
        on clean exit. Mirrors `kdh_repo.calendar_transaction`, keying by the month file.
- [x] **Task 3: Service helpers** (AC: 7, 8)
  - [x] Add `now_iso()`, `today()` (server-side date — the single source for every past/present
        decision) and `is_past(date)` to `services/qotd_service.py`. No endpoints and no answer
        write logic yet (Stories 1.4 / 2.1).
- [x] **Task 4: Tests** (AC: all)
  - [x] `test_qotd_repo.py`: round-trip; empty `Day` for an unseen date; many days share a month
        file (merge preserves all); months-directory scan lists what was written and ignores non-JSON;
        `migrate` passthrough; every unsafe date form rejected; `write_day` creates `months/` on first
        write; `day_exists` distinguishes never-surfaced from empty.
  - [x] `test_qotd_concurrency.py`: N threads upserting answers on one day → all survive; two
        different months not serialized against each other; a raising transaction writes nothing.
  - [x] **Verify the concurrency tests are non-vacuous** by monkeypatching `qotd_repo.key_lock` to a
        no-op and confirming the lost-update tests fail — kept as a permanent regression test
        (`test_lost_update_without_the_lock`) rather than a throwaway harness.

## Dev Notes

**Story 1.8 has landed — consume it, do not rebuild it.** `app/core/locks.py` provides `key_lock(key)`
(reentrant) and `app/core/storage.py` provides `atomic_write_json`. There is no reason to define a
QotD-local lock. [Source: `docs/stories/application-dock-general/for-review/1.8...story.md`]

**⚠️ Keyed by date, like KDH — not by username.** `kdh_repo` is the closest pattern to copy: it keys
files by calendar id and takes no username for file selection. QotD keys by **date**; the username
only picks a slot in `answers`. Copy `kdh_repo`'s structure (validation helper, `migrate`,
transaction context manager). [Source: FR-2, NFR-2]

**Bucketed by month on disk.** Days are stored in one `Month` file per calendar month
(`qotd/months/{YYYY-MM}.json`) rather than a file per day — chosen so the data folder stays small and
legible for a human (~12 files a year, not ~365) and because a tiny group makes a month-granularity
write lock more than sufficient. The public repo API stays day-granular; `day_transaction`/`write_day`
read-modify-write the enclosing month under its file lock, so writing one day never drops the others.

**Validate the date as untrusted.** The date arrives from the URL in later stories (Story 3.3), so
`_validate_date` must reject anything that is not a canonical `YYYY-MM-DD` — mirror
`kdh_repo._validate_calendar_id`. A `datetime.date.fromisoformat` round-trip plus an exact-shape
check is a good belt-and-braces.

**No index file.** `list_days` scans `months/*.json` and flattens (NFR-2/AR-2). An index would be a
second file every answer write must touch — the contention point per-file locking exists to avoid.
Precedent: KDH `list_calendars`, archery `list_sessions`.

**`today()` belongs here.** Stories 2.3 (edit/lock), 3.1 (reveal), 3.2/3.3 (history) all decide
past-vs-present and must agree, and none may trust the client clock (NFR-5). One helper, used
everywhere; `today` is not past (today is still answerable).

**Scope discipline.** No endpoints, no answer upsert, no reveal/lock rules — those are Stories 1.4,
2.1, 2.3, 3.1. This story is the data layer and the shared helpers only.

### Project Structure Notes

- NEW: `backend/app/schemas/qotd.py` (Answer/Day; extends the question models from Story 1.2),
  `backend/app/repositories/qotd_repo.py`, clock helpers in `backend/app/services/qotd_service.py`,
  `backend/tests/test_qotd_repo.py`, `backend/tests/test_qotd_concurrency.py`.
- UPDATE: none expected — `routers/qotd.py` stays a placeholder until Story 1.4.

### References

- [Source: docs/planning-artifacts/epics-question-of-the-day.md#Story 1.3]
- Pattern ref: `docs/stories/kdh/for-review/1.2.shared-data-model-and-repository.story.md`
- Pattern ref: `backend/app/repositories/kdh_repo.py` (structure), with the key changed to the date
- Platform primitives: `backend/app/core/locks.py`, `backend/app/core/storage.py`

## Dev Agent Record

### Completion Notes

- Extended `schemas/qotd.py` with `Answer` (`text`/`rating`/`created_at`/`updated_at`, model
  validator enforcing exactly one of `text`/`rating`), `Day` (`schema_version`, `date`,
  `question_id`, `answers: dict[str, Answer]` keyed by username; `question_id` defaults to `""` so an
  empty day materialises cleanly), and `Month` (`schema_version`, `month`, `days: dict[str, Day]`) —
  the on-disk bucket.
- Created `repositories/qotd_repo.py` keyed by **date**, bucketed into one file per **month**
  (mirrors `kdh_repo`): `_validate_date` (regex `\d{4}-\d{2}-\d{2}` + `date.fromisoformat` round-trip,
  rejecting separators/`..`/padding/impossible dates), `_month_key`/`_month_path`, `read_month`,
  `read_day` (empty `Day` for an unseen date), `write_day` (locked read-modify-write merging the day
  into its month, atomic, creates `months/`), `day_exists` (never-surfaced vs empty), `list_days`
  (months scan flattened, newest-first, ignores non-JSON), `migrate` (v1 passthrough), and
  `day_transaction(date)` holding `key_lock(str(month_path))` across read→yield→write and writing
  nothing if the block raises. No username ever selects a path (AC-3).
- **One file per month, not per day.** Chosen so the data folder stays small and human-legible
  (~12 files/year, not ~365); with a tiny group a month-granularity write lock is ample. The public
  API stays day-granular, so the service/routers/frontend are unchanged.
- Added the shared server clock to `qotd_service.py`: `now_iso()`, `today()` (the single source of
  "today", trivially monkeypatchable), `is_past(date)` (today is not past). Service raises stdlib
  exceptions only.
- Tests: `test_qotd_repo.py` (round-trip, empty-day, many-days-share-a-month-file merge, months scan
  + non-JSON ignore, `migrate`, every unsafe-date form incl. a traversal that touches nothing outside
  the store, `day_exists`, transaction round-trip + write-nothing-on-raise + preserves other days in
  the month) and `test_qotd_concurrency.py` (N=8 concurrent answers on one day all survive; different
  **months** overlap rather than serialize; and a **non-vacuity** test that disables the lock and
  asserts updates are lost — proving the lock is load-bearing).
- **Non-vacuity note:** the story suggested a throwaway harness; I kept it as the permanent
  `test_lost_update_without_the_lock` (monkeypatches `repo.key_lock` to a no-op) for durable
  regression protection, which strictly satisfies AC-6's "verified to fail with the lock disabled".
- Full backend suite green (Windows-only `test_core_storage` concurrency test deselected as noted in
  Story 1.1 — a platform-infra `os.replace` race, green on Linux CI). Frontend untouched.

### File List

- NEW: `backend/app/repositories/qotd_repo.py`
- NEW: `backend/tests/test_qotd_repo.py`
- NEW: `backend/tests/test_qotd_concurrency.py`
- UPDATE: `backend/app/schemas/qotd.py` (added `Answer`, `Day`, `Month`)
- UPDATE: `backend/app/services/qotd_service.py` (added `now_iso`, `today`, `is_past`)

## Change Log

| Date       | Change                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------ |
| 2026-09-06 | Added shared per-day schemas, date-keyed repository with `day_transaction` serialization, server clock helpers, repo + concurrency tests. Status → Ready for Review. |
| 2026-09-07 | Changed on-disk layout from one file per day to one `Month` file per month (`qotd/months/{YYYY-MM}.json`) for a smaller, human-legible data folder; public API unchanged (day-granular). Updated `Month` schema, repo internals, and repo/concurrency tests. |
