---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [2.2]
---

# Story 2.5: Manage a tab's columns

## Status

Ready for Review

## Story

**As** a user,
**I want** to add, rename, retype, reorder and delete columns after creating a tab,
**so that** the sheet can change shape as I learn what I actually need.

## Acceptance Criteria

1. **Add a column.** Given a tab, when I add a column (`POST .../columns` with `{name, type}`), then it is appended in column order with a fresh `c-{uuid8}` id, and every existing row shows it as empty (`—`) with no change to their stored cells. (FR-10, AR-5)

2. **Rename.** Given a column header, when I open its menu and rename the column (`PUT .../columns/{cid}` with `{name}`), then the header updates and no row data changes — cells are keyed by column id, not name.

3. **Retype.** Given a column header, when I change its type, then the endpoint returns the whole updated tab with each cell coerced by the documented rules: any → `text` keeps everything as a string; → `number` keeps values that parse as numbers and blanks the rest; → `date` keeps values that parse as `YYYY-MM-DD` and blanks the rest. Blanked cells become `null` (pruned), and the UI warns before applying a retype that would blank data.

4. **Reorder.** Given a column header, when I move it left or right (`PUT .../columns/order` with the full `{column_ids}` list), then the grid re-renders in the new order and the order persists.

5. **Reorder validation.** Given a `column_ids` payload that is not a permutation of the tab's current columns (missing, extra or duplicated ids), when submitted, then it is rejected with `422` `{detail}` and nothing is written.

6. **Delete.** Given a column header, when I delete the column and confirm, then the column and every row's value for it are removed from my file.

7. **Last column protected.** Given a tab with exactly one column, when I try to delete it, then the API rejects it with `422` `{detail}` and the client does not offer delete — a tab always has at least one column.

8. **Name validation.** Given a rename or add with a blank name or a name that duplicates another column in the same tab, when submitted, then it is rejected with `422` `{detail}`.

9. **Unknown ids.** Given any column operation naming a column id that isn't in that tab, when requested, then the API returns `404` `{detail}`.

## Tasks / Subtasks

- [x] **Task 1: Backend — column CRUD** (AC: 1, 2, 3, 6, 7, 8, 9)
  - [x] `services/listies_service.py`: `add_column`, `update_column` (name and/or type; retype calls `recoerce_column` from 1.2), `delete_column` (last column → `ValueError`), all raising `FileNotFoundError` for unknown ids and `ValueError` for blank/duplicate names.
  - [x] `routers/listies.py`: `POST /sheets/{sid}/tabs/{tid}/columns`, `PUT .../columns/{cid}`, `DELETE .../columns/{cid}` — the mutating ones return the whole `Tab`, delete returns `204`.
  - [x] `schemas/listies.py`: `AddColumnRequest`, `UpdateColumnRequest`, `ReorderColumnsRequest`.
- [x] **Task 2: Backend — reorder** (AC: 4, 5)
  - [x] `reorder_columns(username, sheet_id, tab_id, column_ids)` — validate a strict permutation → `ValueError`; rewrite `order` values.
  - [x] Declare `PUT .../columns/order` **before** `PUT .../columns/{cid}` in the router, or FastAPI matches `order` as a column id.
- [x] **Task 3: Frontend — the header menu** (AC: 1, 2, 3, 4, 6, 7, 8)
  - [x] `components/ColumnHeaderMenu.vue`: rename (inline field), change type (select + a warning when values would be blanked), move left / move right, delete (confirm dialog), with delete hidden when only one column remains.
  - [x] An "add column" affordance at the right edge of the header row, reusing the type select from `ColumnBuilder`.
- [x] **Task 4: Frontend — store** (AC: 1–7)
  - [x] Store actions `addColumn`, `renameColumn`, `retypeColumn`, `moveColumn`, `deleteColumn` — each replaces the active tab with the returned `Tab` (or removes it locally for delete).
- [x] **Task 5: Tests** (AC: 1–9)
  - [x] Backend: add appends and leaves rows untouched; rename leaves cells untouched; each retype direction (text→number, number→text, text→date, date→number, unparseable→`null`); reorder persists; bad permutation → `422`; delete removes cells; last column → `422`; blank/duplicate name → `422`; unknown ids → `404`.
  - [x] Frontend: `ColumnHeaderMenu.spec.ts` (each action, delete hidden with one column, retype warning shown); store specs (tab replaced with the response).

## Dev Notes

**Why the endpoints return a whole `Tab`.** A retype can change many rows and a reorder changes every column's `order`; returning the tab lets the client replace it wholesale instead of reconciling deltas. [Source: spec#API]

**Coercion lives in one place.** `recoerce_column` was built in Story 1.2 and is shared with the write path — do not re-implement the rules here. [Source: Story 1.2, AC 7]

**Route ordering.** `/columns/order` before `/columns/{column_id}`. This is the single most likely silent bug in the story.

**Warn before blanking.** The client compares the current values against `parseCell` for the new type and tells the user how many cells will be emptied before it sends the retype.

### Project Structure Notes

- NEW: `components/ColumnHeaderMenu.vue` + spec.
- UPDATE: `components/SheetGrid.vue`, `stores/useListiesStore.ts` + spec, `routers/listies.py`, `services/listies_service.py`, `schemas/listies.py`, `backend/tests/test_listies_api.py`, `backend/tests/test_listies_coercion.py`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 2.5]
- [Source: docs/superpowers/specs/2026-08-30-listies-design.md#Data model]

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **Backend:** `POST /columns`, `PUT /columns/order`, `PUT /columns/{cid}`, `DELETE /columns/{cid}`. The three mutating endpoints return the whole `Tab` — a retype rewrites rows and a reorder changes every column's `order`, so the client replaces the tab rather than reconciling deltas.
- **Route ordering handled and tested.** `/columns/order` is declared before `/columns/{column_id}`, with `test_order_is_not_mistaken_for_a_column_id` guarding it. This was flagged in the story as the most likely silent bug; it is now the kind that fails loudly.
- **The same order-collision fix as rows** applies to columns: a new column takes `max(order) + 1`, not `len(columns)`, so adding after a delete cannot duplicate a survivor's order. Covered by `test_a_column_added_after_a_delete_still_sorts_last`.
- **Reorder demands a strict permutation** — missing, extra or duplicated ids are all 422, and each rejection is asserted to leave the stored order untouched. A partial list would silently drop columns.
- **Renaming a column to its own name is allowed** (the duplicate check excludes the column being renamed) — otherwise correcting the case of a name would be impossible.
- **The retype warning is computed client-side** by `countBlankedByRetype`, which mirrors the server's rules and recomputes as the target type changes, so the user sees the cost before paying it. The confirm button changes from "Change" to "Change anyway" when values would be lost.
- **Two spec fixes worth noting.** A test asserted a `disable` *attribute* on a stub that consumes `disable` as a prop — rewritten to assert the behaviour (clicking a disabled item emits nothing), which is what actually matters. And the grid spec's `q-btn` stub had no `<slot />`, so the header menu nested inside a button never rendered; the stub now keeps its slot, and `ColumnHeaderMenu` is stubbed by name since it has its own spec.
- **Suites:** backend **664** passed (was 635) — 29 new. Frontend **865** passed plus the one pre-existing `hotaru/LibraryPage` failure. `black`, `ruff` (which re-sorted two import blocks), ESLint, Prettier clean.

### File List
- `backend/app/schemas/listies.py` (modified) — `AddColumnRequest`, `UpdateColumnRequest`, `ReorderColumnsRequest`
- `backend/app/services/listies_service.py` (modified) — `add_column`, `update_column`, `reorder_columns`, `delete_column`, `_check_name_is_free`, `_tab_of`
- `backend/app/routers/listies.py` (modified) — four column routes
- `backend/tests/test_listies_api.py` (modified) — 29 tests added (88 total)
- `frontend/src/apps/listies/coerce.ts` (modified) — `countBlankedByRetype` + spec (24 tests)
- `frontend/src/apps/listies/components/ColumnHeaderMenu.vue` (new) + spec (11 tests)
- `frontend/src/apps/listies/components/SheetGrid.vue` (modified) — header menus, add-column + spec (39 tests)
- `frontend/src/apps/listies/pages/SheetPage.vue` (modified) + spec (11 tests)
- `frontend/src/apps/listies/stores/useListiesStore.ts` (modified) — five column actions + spec (37 tests)
