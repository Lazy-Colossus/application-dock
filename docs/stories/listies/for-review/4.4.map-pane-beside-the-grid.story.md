---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [4.3]
---

# Story 4.4: The map pane beside the grid

## Status

Ready for Review

## Story

**As** a user,
**I want** to open a map next to my grid showing every place in the tab,
**so that** a tab of cafés becomes a map of that city.

## Acceptance Criteria

1. **The toggle.** Given a tab with at least one place column and maps configured, when the tab toolbar renders, then it offers a `Map` toggle; the toggle is absent for a tab with no place column and when maps are not configured. (FR-17, FR-20)

2. **Split and restore.** Given the toggle, when I open it, then the view splits — grid left, map right — and closing it returns the grid to full width. The open state is remembered per tab for the session. (FR-17, NFR-1)

3. **Lazy SDK.** Given the pane opens for the first time, when it loads, then the Maps JS SDK is fetched **once, on demand**, using the browser key from `GET /api/listies/maps-config`; the key is never baked into the bundle and the SDK is not loaded for users who never open a map. (NFR-6)

4. **Markers.** Given the pane is open, when it renders, then it plots one marker per non-empty place cell in the tab; when a tab has more than one place column the markers are colour-coded per column with a small legend. (FR-17)

5. **Bounds.** Given the pane opens, when the markers are plotted, then the map fits its bounds to them; a single place is centred at a sensible zoom rather than fully zoomed in.

6. **Labels.** Given a marker, when it renders, then it is labelled by its row's first text value, falling back to the place's own name when the row has no text column or the value is empty.

7. **Marker → row.** Given a marker, when I click it, then its row is highlighted and the grid scrolls to it. (FR-19)

8. **Row → marker.** Given a row, when I select it in the grid, then the map pans to that row's pin (without changing zoom). (FR-19)

9. **No places.** Given a tab whose place cells are all empty, when the pane opens, then it shows an empty state inviting me to fill a place cell — not a blank world map.

10. **Failure is contained.** Given the SDK fails to load or the key is rejected, when the pane opens, then it shows an error state and the grid keeps working normally. (FR-20)

11. **Live updates.** Given the pane is open, when I fill in or clear a place cell, then its marker appears or disappears without reopening the pane, and the bounds are not re-fitted.

## Tasks / Subtasks

- [x] **Task 1: SDK loader** (AC: 3, 10)
  - [x] `apps/listies/maps.ts`: `loadMapsSdk(browserKey)` — injects the script once, returns a shared promise, rejects cleanly on error. Pure enough to unit-test with a stubbed DOM.
- [x] **Task 2: The pane** (AC: 4, 5, 6, 7, 8, 9, 10, 11)
  - [x] `components/MapPane.vue`: create the map, plot markers from the tab's place cells, colour by column, fit bounds on open, emit `marker-click`, accept a `selectedRowId` to pan to, render empty and error states, and reconcile markers when the cell data changes.
- [x] **Task 3: The split layout** (AC: 1, 2)
  - [x] `SheetPage.vue`: a `Map` toggle in the tab toolbar (visible only when the tab has a place column and `mapsEnabled`), a two-pane layout when open, full-width grid when closed, and per-tab open state held in the store for the session.
  - [x] `SheetGrid.vue`: accept a `highlightedRowId`, scroll it into view, and emit row selection.
- [x] **Task 4: Tests** (AC: 1–11)
  - [x] `maps.spec.ts`: the script is injected once across concurrent calls; a load failure rejects.
  - [x] `MapPane.spec.ts` (Google SDK stubbed): a marker per non-empty place, colour per column, bounds fitted on open, marker click emits the row id, `selectedRowId` pans, empty state, error state, markers reconcile on data change without re-fitting.
  - [x] `SheetPage.spec.ts`: toggle hidden without a place column and when maps are disabled; opening splits; closing restores; state remembered per tab.

## Dev Notes

**Toggleable split, deliberately.** The split view is the chosen shape, but it is opened rather than permanent: a wide grid keeps its width when you are not looking at a map, and the Maps SDK — the heaviest thing this app loads — is only fetched when someone actually asks for a map. [Source: spec#Interaction design]

**Never re-fit on data change.** Re-fitting the bounds while someone is editing makes the map lurch. Fit on open and on the explicit control (4.5); otherwise leave the viewport alone. [Source: AC 11, Story 4.5]

**Stub the SDK in tests.** No test loads Google's script. `maps.ts` is the seam: inject a fake `google.maps` in specs.

### Project Structure Notes

- NEW: `apps/listies/maps.ts` + spec, `components/MapPane.vue` + spec.
- UPDATE: `pages/SheetPage.vue` + spec, `components/SheetGrid.vue` + spec, `stores/useListiesStore.ts`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 4.4]

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **`maps.ts`** loads the SDK once, on demand, with the runtime browser key: a shared promise so several callers cannot inject two script tags, an encoded key, and a cache that is cleared on failure so a later attempt can retry rather than replaying the same rejection.
- **A real race, found by running it.** With `loading=async`, `google.maps` appears **before it is usable** — the first (cold) open produced an unhandled rejection and a blank box, while a warm reload worked fine, which is exactly the shape of bug that unit tests never see. `loadMapsSdk` now awaits `importLibrary("maps")` when the SDK offers it, and `MapPane` wraps map construction in a `try/catch` so a failure becomes a visible message rather than a silent rejection. Both have tests.
- **A CSS ordering bug, also only visible in a browser.** `.listies-sheet__body--split { flex-direction: row }` was declared *before* the base `.listies-sheet__body { flex-direction: column }`. Equal specificity, so source order decided and the base rule won: the pane rendered *below* the grid, full width. Moved after the base rule, with a comment saying why the order matters.
- **The pane is closed until asked for**, per tab, for the session. The SDK is the heaviest thing this app loads and most tabs never want it; a test asserts nothing is loaded uninvited.
- **Fit on open, never on change.** A single place is centred at a sensible zoom rather than fitted (fitting bounds to one point zooms to maximum). Editing a cell moves pins but never the viewport — there are explicit tests that neither a data change nor a pan re-fits, because a map that lurches while you type is worse than one that is slightly off.
- **Markers reconcile by key** (`rowId:columnId`): a filled place adds one, a cleared place detaches one, and nothing else is rebuilt.
- **Verified live** on a cache-ignoring cold reload: SDK loads, two pins plot and the map fits to them, grid and map sit side by side (694px / 460px), no unhandled rejections, and clicking a row highlights it and pans the map.
- **`Marker` over `AdvancedMarkerElement`** deliberately: the newer API needs a cloud-configured Map ID, which would make the feature depend on console setup beyond the two keys. Noted in the code.
- **Suites:** frontend **1088** passed plus the one pre-existing `hotaru/LibraryPage` failure; backend unchanged at **765**. ESLint, Prettier and `vue-tsc` clean, and the map spec's script injection is intercepted so no test touches the network.

### File List
- `frontend/src/apps/listies/maps.ts` (new) + spec (13 tests)
- `frontend/src/apps/listies/components/MapPane.vue` (new) + spec (21 tests)
- `frontend/src/apps/listies/components/SheetGrid.vue` (modified) — `select-row`, `highlightedRowId` + spec (64 tests)
- `frontend/src/apps/listies/pages/SheetPage.vue` (modified) — map toggle, split layout, per-tab open state + spec (38 tests)
