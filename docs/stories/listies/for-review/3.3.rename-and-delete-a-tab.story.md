---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [3.1]
---

# Story 3.3: Rename and delete a tab

## Status

Ready for Review

## Story

**As** a user,
**I want** to rename a tab or remove one I no longer need,
**so that** the sheet's tabs keep matching what's actually in them.

## Acceptance Criteria

1. **Rename.** Given a tab chip, when I open its menu and rename it (`PUT .../tabs/{tid}` with `{name}`), then the name updates in my file and in the bar. (FR-14)

2. **Rename validation.** Given a blank or whitespace name, when submitted, then the client disables submit and the API rejects it with `422` `{detail}`.

3. **Delete with confirmation.** Given a tab chip, when I choose delete and confirm, then `DELETE .../tabs/{tid}` removes the tab, its columns and all its rows from my file; if it was the active tab, the grid switches to its neighbour (the next tab, or the previous one if it was last). (FR-14)

4. **The confirmation names the cost.** Given the confirm dialog, when it opens, then it names the tab and how many rows will be destroyed.

5. **Last tab protected.** Given a sheet with exactly one tab, when I try to delete that tab, then the API rejects it with `422` `{detail}` and the client does not offer delete — a sheet always has at least one tab. (FR-14)

6. **Unknown id.** Given a rename or delete for a tab id that isn't in this sheet, when requested, then the API returns `404` `{detail}`.

7. **Order preserved.** Given the remaining tabs, when one is deleted, then their relative order in the bar is unchanged.

## Tasks / Subtasks

- [x] **Task 1: Backend** (AC: 1, 2, 3, 5, 6, 7)
  - [x] `services/listies_service.py`: `update_tab(username, sheet_id, tab_id, name)` (blank → `ValueError`) and `delete_tab(username, sheet_id, tab_id)` (last tab → `ValueError`; unknown → `FileNotFoundError`).
  - [x] `routers/listies.py`: `PUT /sheets/{sid}/tabs/{tid}` → `Tab`, `DELETE /sheets/{sid}/tabs/{tid}` → `204`.
  - [x] `schemas/listies.py`: `UpdateTabRequest` (all-optional; nothing updatable → `422`).
- [x] **Task 2: Frontend** (AC: 1, 3, 4, 5)
  - [x] A per-chip menu in `TabBar.vue` (rename / delete), with delete hidden when only one tab remains.
  - [x] Store: `renameTab(tabId, name)`, `deleteTab(tabId)` — the latter picks the neighbour as the new `activeTabId` and clears the sort.
- [x] **Task 3: Tests** (AC: 1–7)
  - [x] Backend: rename persists; blank → `422`; delete removes the tab and its rows; last tab → `422`; unknown id → `404`; remaining order intact.
  - [x] Frontend: `TabBar.spec.ts` — menu actions, delete hidden for a single-tab sheet, confirm required and it states the row count, active tab falls back to the neighbour.

## Dev Notes

**Neighbour selection.** Deleting the active tab must always leave a valid `activeTabId` — next tab, or previous if the deleted one was last. AC 5 guarantees there is always at least one left.

**Say what will be lost.** Deleting a tab destroys every row in it with no undo; the confirm dialog carries the row count rather than a generic "are you sure". [Source: platform convention, Story 1.4]

### Project Structure Notes

- UPDATE: `components/TabBar.vue` + spec, `stores/useListiesStore.ts` + spec, `routers/listies.py`, `services/listies_service.py`, `schemas/listies.py`, `backend/tests/test_listies_api.py`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 3.3]

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **Backend:** `PUT /sheets/{sid}/tabs/{tid}` (trims, 422 on blank or an empty body, 404 on an unknown tab) and `DELETE` (204; **422 for the last tab** — a sheet with no tabs would have nowhere to put a row and no columns to define one).
- **The order-collision guard held.** `test_a_tab_added_after_a_delete_still_sorts_last` passed first time, because `create_tab` was already written with `max(order) + 1` after the row (2.4) and column (2.5) versions of this bug. The test is kept as a regression guard rather than removed for being green.
- **Neighbour selection on delete:** the next tab along, or the previous one when the deleted tab was last. Three tests cover next / previous / "deleting a different tab leaves the active one alone" — the case that is easy to break by resetting the active tab unconditionally.
- **The client refuses to delete the only tab without asking the server.** The server enforces it too, but there is no reason to make a request that cannot succeed; the bar simply does not offer delete when there is one tab, while rename stays available.
- **The confirmation states the cost** — the tab's name and how many rows go with it — rather than a generic "are you sure".
- **Suites:** backend **698** passed (was 685) — 13 new. Frontend **940** passed plus the one pre-existing `hotaru/LibraryPage` failure. `black`, `ruff`, ESLint, Prettier clean.

### File List
- `backend/app/schemas/listies.py` (modified) — `UpdateTabRequest`
- `backend/app/services/listies_service.py` (modified) — `update_tab`, `delete_tab`
- `backend/app/routers/listies.py` (modified) — `PUT`/`DELETE /sheets/{sheet_id}/tabs/{tab_id}`
- `backend/tests/test_listies_api.py` (modified) — 13 tests added (122 total)
- `frontend/src/apps/listies/components/TabBar.vue` (modified) — per-chip menu, inline rename, delete confirm + spec (13 tests)
- `frontend/src/apps/listies/pages/SheetPage.vue` (modified) + spec (19 tests)
- `frontend/src/apps/listies/stores/useListiesStore.ts` (modified) — `renameTab`, `deleteTab` + spec (50 tests)
