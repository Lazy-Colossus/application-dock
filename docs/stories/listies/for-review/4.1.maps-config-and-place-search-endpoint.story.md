---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [1.1]
---

# Story 4.1: Maps configuration and the place-search endpoint

## Status

Ready for Review

## Story

**As** the platform,
**I want** Google credentials in server configuration and place search proxied through our own API,
**so that** the server key never reaches the browser, searches are cached, and an unconfigured deployment degrades cleanly instead of breaking.

## Acceptance Criteria

1. **Settings.** Given `app/core/config.py`, when settings load, then `google_maps_server_key` and `google_maps_browser_key` are read from the environment (both defaulting to `""`), and `docker-compose.yml` passes `GOOGLE_MAPS_SERVER_KEY` / `GOOGLE_MAPS_BROWSER_KEY` through. (AR-6, FR-20)

2. **Config endpoint.** Given `GET /api/listies/maps-config` called by an authenticated user, when both keys are configured, then it returns `{ enabled: true, browser_key: "…" }`; otherwise `{ enabled: false }` with no key field. It **never** returns the server key. (NFR-6)

3. **Search endpoint.** Given `GET /api/listies/places/search?q=…`, when maps are configured, then `services/places_service.py` calls the Google Places **Text Search** endpoint with the server key and returns a normalized `list[PlaceResult]` — `place_id`, `name`, `address`, `lat`, `lng`. It is the only module in the codebase that talks to Google. (AR-6, FR-16)

4. **Caching.** Given a repeated `(q, near)` within the cache TTL, when requested, then it is served from an in-process cache with no second upstream call; the cache is bounded in entry count and entries expire. (NFR-6)

5. **Location bias.** Given `near=lat,lng`, when searching, then the upstream request biases results toward that point; an unparseable `near` is ignored rather than fatal.

6. **Unconfigured.** Given the keys are unset, when `/places/search` is called, then it returns `503` with a `{detail}` naming the missing configuration; nothing crashes and no other app is affected. (FR-20)

7. **Upstream failure.** Given an upstream non-2xx, transport error or timeout, when searching, then the endpoint returns `502` `{detail}` — never a stack trace, never the key, and never a partial result.

8. **Blank query.** Given a blank or whitespace `q`, when requested, then it returns `422` `{detail}` without calling Google.

9. **Auth.** Given either endpoint without a valid token, when called, then it is rejected by `Depends(get_current_user)` like every other route. (NFR-4)

10. **No live calls in tests.** Given the test suite, when it runs, then no test performs a real network call — `httpx` is stubbed — and `httpx` has moved from `requirements-dev.txt` into `requirements.txt`.

## Tasks / Subtasks

- [x] **Task 1: Configuration** (AC: 1, 10)
  - [x] Add both keys to `Settings` in `backend/app/core/config.py`.
  - [x] Add the two env vars to `docker-compose.yml`; document them in the README's configuration section.
  - [x] Move `httpx` from `requirements-dev.txt` to `requirements.txt` (pin the same version).
- [x] **Task 2: The places service** (AC: 3, 4, 5, 6, 7, 8)
  - [x] `backend/app/services/places_service.py`: `maps_enabled()`, `browser_key()`, `search(q, near=None) -> list[PlaceResult]`.
  - [x] Call `POST https://places.googleapis.com/v1/places:searchText` with `X-Goog-Api-Key` and an `X-Goog-FieldMask` limited to the five fields we keep (a narrower mask is a cheaper billing tier); a short timeout; `locationBias` when `near` parses.
  - [x] Normalize the upstream payload to `PlaceResult`; raise `RuntimeError` for unconfigured, `ValueError` for a blank query, and a dedicated upstream error for transport/non-2xx.
  - [x] TTL + max-size cache keyed by `(q.strip().lower(), near)` — a plain dict with timestamps; no new dependency.
- [x] **Task 3: Router** (AC: 2, 3, 6, 7, 8, 9)
  - [x] `GET /api/listies/maps-config` → `MapsConfig`, `GET /api/listies/places/search` → `list[PlaceResult]`, both `Depends(get_current_user)`; translate `RuntimeError` → `503`, `ValueError` → `422`, upstream error → `502`.
  - [x] `schemas/listies.py`: `PlaceResult`, `MapsConfig`.
- [x] **Task 4: Tests** (AC: 1–10)
  - [x] `backend/tests/test_listies_places.py` with a stubbed `httpx` transport: normalization of a realistic payload; cache hit avoids a second call; expiry causes a refetch; `near` reaches the request body; unconfigured → `503`; upstream 500 / timeout → `502`; blank `q` → `422`; unauthenticated → rejected.
  - [x] An explicit assertion that no response body of either endpoint contains the server key.

## Dev Notes

**Why proxy at all.** The server key stays server-side and searches are cached, so retyping a query does not re-bill; the browser key is separately referrer-restricted and handed to the SPA at runtime so it is never baked into a build artifact. [Source: NFR-6]

**Field mask is a cost lever.** Google bills Text Search by the SKU implied by the fields requested. Ask only for `places.id`, `places.displayName`, `places.formattedAddress`, `places.location`.

**Degrade, don't crash.** This is the platform's first optional external credential. `enabled: false` must be a completely ordinary state — the frontend hides the place type and the map, and nothing else in the dock notices. [Source: FR-20]

**Scope discipline.** Backend only. No `place` column type (4.2), no cell UI (4.3), no map (4.4).

### Project Structure Notes

- NEW: `backend/app/services/places_service.py`, `backend/tests/test_listies_places.py`.
- UPDATE: `backend/app/core/config.py`, `backend/app/routers/listies.py`, `backend/app/schemas/listies.py`, `backend/requirements.txt`, `backend/requirements-dev.txt`, `docker-compose.yml`, `README.md`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 4.1]
- [Source: docs/superpowers/specs/2026-08-30-listies-design.md#API]

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **`services/places_service.py`** is the only module that talks to Google. It exposes `maps_enabled`, `browser_key`, `search`, plus `clear_cache` / `cache_size` for tests, and raises `MapsNotConfiguredError` / `ValueError` / `PlacesUpstreamError` — the router turns those into 503 / 422 / 502 and nothing in the service raises `HTTPException`.
- **No test touches the network.** `httpx.Client` is swapped for one built on `httpx.MockTransport`, so the real request construction is still exercised — the tests assert the upstream host, the `X-Goog-Api-Key` header, the query in the body, and the exact field mask — while the response is ours. That is stronger than stubbing our own wrapper function, which would have tested nothing about how we call Google.
- **The field mask is asserted as a set**, because it is a billing lever rather than a formality: Text Search is charged by the fields requested, so asking for exactly the five a place cell stores keeps it on the cheapest SKU. A future careless addition to that mask now fails a test.
- **The key cannot leak.** Two explicit tests: `maps-config` never contains the server key, and an upstream 403 (whose body can echo the key back) never reaches the client — the message is ours, the upstream detail is dropped.
- **Both keys or nothing.** `enabled` is false unless *both* are set, because a browser key without a server key gives a map that cannot search, and the reverse gives search with nowhere to plot it. Tested with only one set.
- **A malformed `near` is ignored, not fatal** — an unbiased search is still useful, and failing the whole request over a hint would be worse. Out-of-range coordinates are dropped the same way.
- **Failures are not cached**, so a transient 500 does not poison the next minute of searches. Tested.
- **The cache is keyed on the case-folded, trimmed query plus the parsed point**, bounded to `CACHE_MAX_ENTRIES` with oldest-first eviction, and expires by a `_now()` seam so the test advances the clock instead of sleeping.
- **`httpx` moved from `requirements-dev.txt` to `requirements.txt`** (it was previously only a test dependency, pulled in by `TestClient`), the two env vars are passed through in `docker-compose.yml`, and the README gained a short section explaining that both keys are optional and how to restrict each.
- **Suites:** backend **739** passed (was 710) — 29 new. `black` and `ruff` clean. No frontend work in this story.

### File List
- `backend/app/services/places_service.py` (new)
- `backend/app/core/config.py` (modified) — the two keys
- `backend/app/schemas/listies.py` (modified) — `PlaceResult`, `MapsConfig`
- `backend/app/routers/listies.py` (modified) — `GET /maps-config`, `GET /places/search`
- `backend/tests/test_listies_places.py` (new) — 29 tests
- `backend/requirements.txt` / `requirements-dev.txt` (modified) — `httpx` promoted
- `docker-compose.yml` (modified) — env passthrough
- `README.md` (modified) — optional Maps configuration
