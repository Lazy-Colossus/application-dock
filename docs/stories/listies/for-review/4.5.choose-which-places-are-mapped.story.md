---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [4.4]
---

# Story 4.5: Choose which places are on the map

## Status

Ready for Review

## Story

**As** a user,
**I want** to tick and untick which places are currently plotted,
**so that** I can narrow the map to the handful I am actually deciding between.

## Acceptance Criteria

1. **The checkbox column.** Given the map pane is open, when the grid renders, then it grows a leading "on map" checkbox column with one tick per row holding at least one non-empty place; rows with no place have no checkbox. (FR-18)

2. **All on by default.** Given the pane opens, when it first renders, then every place row is ticked.

3. **Instant effect.** Given a ticked row, when I untick it, then its marker disappears immediately, and re-ticking restores it — no request is made either way. (FR-18)

4. **All / none and a counter.** Given the pane header, when it renders, then it offers **all** and **none** controls and a "4 of 7 shown" counter that tracks the ticks.

5. **No jumping.** Given a tick change, when the markers update, then the map does **not** re-fit its bounds; an explicit "fit to shown" control re-fits on demand.

6. **View state only.** Given the ticks, when I switch tab, reload, or close the pane, then they reset to all-on — like sorting, this is never written to the file.

7. **Closed pane, clean grid.** Given the map pane is closed, when the grid renders, then the checkbox column is gone and the grid is back to full width.

8. **New rows.** Given a place added to a row while the pane is open, when it appears, then that row is ticked by default and its marker is plotted.

## Tasks / Subtasks

- [x] **Task 1: Tick state** (AC: 1, 2, 6, 8)
  - [x] Hold `mappedRowIds` (a `Set`) beside the sort spec — reset on tab switch and on pane close; a row gaining its first place is added automatically.
- [x] **Task 2: Grid column** (AC: 1, 3, 7)
  - [x] `SheetGrid.vue`: render the leading checkbox column only while the pane is open; no checkbox for a row with no place; the tick must be keyboard-reachable and not disturb the cell focus model.
- [x] **Task 3: Pane header** (AC: 4, 5)
  - [x] `MapPane.vue`: all / none controls, the "N of M shown" counter, and a "fit to shown" control that re-fits the bounds on demand.
  - [x] Plot only the ticked rows; do not re-fit on a tick change.
- [x] **Task 4: Tests** (AC: 1–8)
  - [x] `SheetGrid.spec.ts`: column present only with the pane open, no checkbox without a place, ticking emits and sends no request, keyboard reachable.
  - [x] `MapPane.spec.ts`: only ticked rows are plotted, all / none work, the counter is right, "fit to shown" re-fits, a tick change does not.
  - [x] Store spec: ticks reset on tab switch and pane close; a newly filled place row starts ticked.

## Dev Notes

**View state, like sorting.** Ticks are how the map is being read right now, not a property of the data — so they live in the store, never in the file, and reset with the tab. This mirrors the sort decision in Story 2.6 and keeps the document free of UI state. [Source: Story 2.6, FR-18]

**Don't fight the cell focus model.** The checkbox column sits outside the cell grid: Tab within the grid must still walk cells (Story 2.3), while the checkbox stays reachable in the normal document order.

**Bounds are user-controlled.** Auto-fitting on every tick makes the map lurch while you triage; fitting stays on the open and the explicit control. [Source: Story 4.4 AC 11]

### Project Structure Notes

- UPDATE: `components/SheetGrid.vue` + spec, `components/MapPane.vue` + spec, `stores/useListiesStore.ts` + spec, `pages/SheetPage.vue`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 4.5]

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **The grid grows a leading tick column only while the map is open** (`mappedRowIds === null` means closed, so no column at all), and only rows that actually hold a place get a tick — a row with nothing to plot has nothing to toggle. The ghost row keeps an empty cell so the columns stay aligned.
- **The state tracks what is *hidden*, not what is shown.** This is the one decision worth arguing about: a set of shown ids would have to be updated every time a place is added, and anything missed would silently stay off the map. Tracking exceptions means a place filled in while the map is open is plotted **without being asked for** — there is a test that pushes a new place row and asserts it appears.
- **Nothing is written.** Ticks live in the page, never in the document — the same call as the sort in Story 2.6. A test asserts no `PUT` and no `POST` on toggle, and they reset with the tab.
- **A tick never re-frames the map.** `syncMarkers` follows the shown subset while `fitToPins` is called only on open and from the explicit **Fit** control — re-framing on every tick would make the map jump while you are triaging. Tested both ways round.
- **The counter names the denominator honestly**: "1 of 2 shown" counts rows that hold a place, not every row in the tab.
- **Verified live** with the real map: two ticks, all on by default, unticking one dropped its pin and the counter moved to "1 of 2 shown", None → "0 of 2", All → "2 of 2", and Fit re-framed without breaking the map.
- **Suites:** frontend **1114** passed plus the one pre-existing `hotaru/LibraryPage` failure; backend unchanged at **765**. ESLint, Prettier and `vue-tsc` clean.

### File List
- `frontend/src/apps/listies/components/MapPane.vue` (modified) — shown subset, header with counter, All / None / Fit + spec (30 tests)
- `frontend/src/apps/listies/components/SheetGrid.vue` (modified) — the tick column + spec (69 tests)
- `frontend/src/apps/listies/pages/SheetPage.vue` (modified) — hidden-row state, reset on tab change + spec (46 tests)
