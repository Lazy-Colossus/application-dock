---
baseline_commit: 9ba20dae6ab39383f5ede85a7d3c66e937497e21
depends_on: [1.1]
---

# Story 1.2: Per-user data model, repository & schemas

## Status

Ready for Review

## Story

**As** the platform,
**I want** a JSON file per user with an atomic-write repository and typed schemas,
**so that** every user's sheets, tabs, columns and rows are stored and isolated correctly.

## Acceptance Criteria

1. **Empty document on first read.** Given `repositories/listies_repo.py`, when a user's document is read and they have no file yet, then it returns `{ schema_version: 1, sheets: [] }` without error. (AR-2)

2. **Repo is the only filesystem access.** Given any write, when it happens, then it goes through `_atomic_write_json` (write-`.tmp`-then-`os.replace`) and `mkdir(parents=True, exist_ok=True)` creates `users/` on first write; no service or router touches the filesystem. (NFR-3, AR-3)

3. **Path safety.** Given a username containing `/`, `\`, `.` or `..`, when the repo derives a path, then it raises `ValueError` rather than writing outside `DATA_DIR/listies/users/`. (NFR-2)

4. **Schemas.** Given `schemas/listies.py`, when models are defined, then Pydantic v2 models exist for `Column` (`id`, `name`, `type: Literal["text","number","date"]`, `order`), `Row` (`id`, `order`, `cells: dict[str, str | int | float | None]` — `int` before `float` so a whole number stays a whole number on round-trip, `created_at`, `updated_at`), `Tab` (`id`, `name`, `order`, `columns`, `rows`), `Sheet` (`id`, `name`, `created_at`, `tabs`) and `ListiesDoc` (`schema_version`, `sheets`). (AR-3, AR-5)

5. **Migration hook.** Given `migrate(raw)`, when a document is read, then it runs before validation; v1 is a pass-through, so future `schema_version` bumps have one home.

6. **Id minting and timestamps.** Given anything created, when the service mints it, then it gets a stable id (`s-`/`tb-`/`c-`/`r-` + 8 hex chars) and ISO-8601 UTC timestamps; ids are unique within the document.

7. **Type coercion helpers.** Given `services/listies_service.py`, when a cell value is written or a column is retyped, then shared helpers `coerce_value(value, column_type)` (raises `ValueError` on a value that does not fit) and `recoerce_column(rows, column_id, new_type)` (keeps what parses, blanks the rest) implement the documented rules — one implementation used by both paths.

8. **Layer purity.** Given the service, when it fails, then it raises stdlib exceptions (`FileNotFoundError` for a missing sheet/tab/column/row, `ValueError` for invalid input) and never `HTTPException`. (NFR-4)

9. **Round-trip test.** Given a document with a sheet, a tab, columns of all three types and rows with `null` and non-`null` cells, when written and read back, then it validates and is byte-stable in content.

## Tasks / Subtasks

- [x] **Task 1: Schemas** (AC: 4, 5)
  - [x] Create `backend/app/schemas/listies.py` with the models above, `ColumnType = Literal["text","number","date"]`, and sensible defaults so a fresh sheet validates without ceremony.
- [x] **Task 2: Repository** (AC: 1, 2, 3, 5)
  - [x] Create `backend/app/repositories/listies_repo.py` with `_validate_username`, `_user_path`, `migrate`, `read_doc`, `write_doc` — mirroring `context_switch_repo.py`.
- [x] **Task 3: Service foundations** (AC: 6, 7, 8)
  - [x] Create `backend/app/services/listies_service.py` with `_now_iso`, `_new_id(prefix)`, `coerce_value`, `recoerce_column`, and the private lookup helpers (`_find_sheet`, `_find_tab`, `_find_column`, `_find_row`) that raise `FileNotFoundError`.
- [x] **Task 4: Tests** (AC: 1, 3, 7, 9)
  - [x] `backend/tests/test_listies_repo.py`: empty-document read, round-trip, `users/` created on write, unsafe usernames rejected (`tmp_path` + `monkeypatch` on `settings.data_dir`).
  - [x] `backend/tests/test_listies_coercion.py`: every value/type pair, `null` handling, empty-string→`null`, and each retype direction.

## Dev Notes

**Cells keyed by column id.** `Row.cells` is `dict[column_id, value]`. Never index- or name-keyed — this is what makes rename/reorder/delete of columns non-destructive. Absent key ≡ `null`; the service prunes `null` entries and entries for columns that no longer exist so files stay small. [Source: spec#Data model, AR-5]

**`null` is not `0` or `""`.** Empty means "not filled in". A text value that is empty or whitespace normalizes to `null`. Same rule archery uses for unentered shots. [Source: CLAUDE.md#Sessions data model]

**Atomic writes.** Reuse `session_repo._atomic_write_json`, exactly as `context_switch_repo` does; extracting it to a shared core util is tracked separately and out of scope here.

**Scope discipline.** No routes and no endpoints in this story beyond what Story 1.1 mounted. Sheet creation lands in 1.3.

### Project Structure Notes

- NEW: `backend/app/schemas/listies.py`, `backend/app/repositories/listies_repo.py`, `backend/app/services/listies_service.py`, `backend/tests/test_listies_repo.py`, `backend/tests/test_listies_coercion.py`.

### References

- [Source: docs/planning-artifacts/epics-listies.md#Story 1.2]
- [Source: docs/superpowers/specs/2026-08-30-listies-design.md#Data model]
- Pattern ref: `backend/app/repositories/context_switch_repo.py`, `backend/app/schemas/context_switch.py`

## Dev Agent Record

### Agent Model Used
claude-opus-5[1m]

### Completion Notes
- **Schemas** (`app/schemas/listies.py`): `ColumnType = Literal["text","number","date"]`, `CellValue = str | int | float | None`, and `Column` / `Row` / `Tab` / `Sheet` / `ListiesDoc`. `Row.cells` is `dict[column_id, CellValue]` — keyed by id, per AR-5. An `int` stays an `int` through a write/read round-trip (asserted, not assumed).
- **Repository** (`app/repositories/listies_repo.py`): `_validate_username` / `_user_path` / `migrate` / `read_doc` / `write_doc`, mirroring `context_switch_repo`. Reads a missing file as an empty document *without* creating it; writes `mkdir(parents=True, exist_ok=True)` then go through `session_repo._atomic_write_json`.
- **Service foundations** (`app/services/listies_service.py`): `now_iso`, `new_id(prefix)`, `coerce_value`, `recoerce_column`, and `find_sheet` / `find_tab` / `find_column` / `find_row` raising `FileNotFoundError`. `_reparse` is the single retype conversion, used only by `recoerce_column`, so the strict write path and the lenient retype path share one definition of each type.
- **Coercion decisions worth noting.** `number` rejects a numeric *string* (`"12"`) on the write path — the client parses before sending, and accepting both would make "what is stored" ambiguous. `bool` is rejected explicitly because it is an `int` subclass in Python. A retype is deliberately more forgiving than a write: `"12"` → `12` there, since the point is to keep what can be kept. Blanked cells are **pruned** from `cells`, not stored as `null`.
- **TDD:** both test files were written first and watched fail on `ImportError` before any module existed.
- **Suites:** backend **576** passed (was 530) — 46 new. `black` and `ruff` clean (ruff pushed `isinstance(x, (a, b))` → `isinstance(x, a | b)`).
- No frontend work in this story; no routes or endpoints beyond the router Story 1.1 mounted.

### File List
- `backend/app/schemas/listies.py` (new)
- `backend/app/repositories/listies_repo.py` (new)
- `backend/app/services/listies_service.py` (new)
- `backend/tests/test_listies_repo.py` (new) — 20 tests
- `backend/tests/test_listies_coercion.py` (new) — 26 tests
