# Directory where session JSON files are stored.
# In Docker: defaults to /data, mounted as a named volume (archery-data).
# Outside Docker (local dev): override to a writable path, e.g. ./local-data
DATA_DIR=/data
