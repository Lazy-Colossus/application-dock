# Backend-only overrides.
#
# The backend reads the repository-root `.env` first and this file second, so
# anything here wins. Most settings belong in the root `.env`; this is for
# local-only overrides — typically DATA_DIR, since /data is not writable
# outside Docker.
#
# DATA_DIR=./local-data
